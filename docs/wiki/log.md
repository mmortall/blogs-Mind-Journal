# Wiki log

## [2026-08-30] ingest | Mind Journal repository

Created initial project wiki, source map, agent map, concept page, article
catalog, and open-question register from README, Hugo configuration, and ten
English/Russian posts. Added Cerebro routing files; Kaneo provisioning remained
blocked by Cloudflare Tunnel error 1033.

Committed local Cerebro integration as `ba5a3f2`; central catalog validation can
now resolve the exact repository revision. Created and read back dedicated
Kaneo project `ji0wvag33gbpawnxd9nkan0s`; added repo-local managed lifecycle.

## [2026-08-31] research | Governance of large groups

Added `docs/wiki/research/governance-of-groups.md`: a practical research map
covering coordination problems, collective action, state formation, culture,
religion, organizations, information, new religious movements, and design
questions for Sytrism. Added a source-backed reading path and linked the map
from the Wiki index and source map.

Created Kaneo sub-epic `#15` with thematic tasks `#16–#22`; framework task `#14`
was completed after report creation and snapshot validation. Main research
continues under sub-epic `#15`.

Comparative checkpoint: added Nepal irrigation and forestry, large-group
experiments, climate polycentricity, and digital commons. Updated Olson’s
boundary conditions and opened Kaneo leaf `#23` for this evidence pass.

State-capacity checkpoint: expanded state section into an eight-contour model
(legibility, extraction, coercion, implementation, adjudication, legitimacy,
constraints, feedback), added a historical feedback loop, comparative table,
formalization template, and H4 falsifiers. Added sources on endogenous state
capacity, taxation and information, European war-state formation, China/Europe
divergence, and cross-cultural state origins. Opened and completed managed leaf
`#24`; research sub-epic `#15` remains in progress and thematic task `#17` is
complete.

Culture/NRM checkpoint: expanded culture as an operational part of the rule
system, added a mechanism table for norms, reputation, identity, ritual,
moral narratives, organizations, and commitment signals, plus a lifecycle model
for norm change. Expanded NRM analysis into contact, socialization, commitment,
hierarchy, discipline, conflict, exit, and abuse; separated coercive control
from universal brainwashing claims. Added comparative sources on norm dynamics,
cultural group selection, moralizing-god debates, conversion, and abuse in NRMs.
Opened managed leaf `#26`, moved it under research sub-epic `#15`, and completed
it after canonical validation.

Corporate-management checkpoint: expanded market, hierarchy, network, and
hybrid governance into a transaction-level choice model using measurability,
asset specificity, uncertainty, frequency, and knowledge location. Added
Ouchi's market/bureaucracy/clan matrix, knowledge-based hierarchy, Competing
Values culture poles, exploration/exploitation tension, and manager-facing
metrics and failure modes. Opened managed leaf `#28` and moved it under
research sub-epic `#15`; completed it after canonical validation.

Information-feedback checkpoint: expanded collective intelligence into an
observation–validation–aggregation–decision–measurement loop. Added conditions
for useful signals, information cascades, hierarchy filters, correlated error,
metric gaming, silence, accountability, appeals, near-miss learning, and
psychological safety. Added task-specific procedures for factual estimates,
value conflicts, expert problems, crises, and high-risk systems. Opened managed
leaf `#29` and moved it under research sub-epic `#15`; completed it after
canonical validation.

Sytrism-design checkpoint: converted research findings into prioritized P0–P2
growth points: split decision contours, reversible delegation, voice/exit,
polycentric levels, metric portfolios, institutionalized dissent, evidence-bound
AI, and culture with anti-cult safeguards. Added a minimum pilot protocol,
success metrics, red-team questions, and sources on polycentric governance,
Hirschman's exit/voice, reviewable automated decisions, third-party oversight,
and experimentation/accountability. Opened managed leaf `#31` and moved it
under research sub-epic `#15`; completed it after canonical validation.

NRM checkpoint: expanded closed-group analysis with a phase model (contact,
socialization, commitment, hierarchy, discipline, conflict, exit, abuse), an
exit-cost vector, and comparative cases covering Shakers, Oneida, Millerites,
People's Temple/Jonestown, and Aum Shinrikyo. Kept recruitment/network evidence
separate from abuse claims and treated brainwashing as a contested, limited
concept rather than a universal explanation. Opened managed leaf `#32`, moved
it under research sub-epic `#15`, and completed it after canonical validation.

Comparative-cases checkpoint: added a seven-case matrix linking coordination
problem, mechanism, indicators, failure mode, and transfer limit. Added China’s
`Keju`, Wikipedia/FOSS, and polycentric climate governance as contrasts to local
commons, European state formation, industrial firms, and NRMs. Added a reusable
`Case` passport so future articles can compare institutions without turning one
successful mechanism into a universal recipe. Opened managed leaf `#34` under
sub-epic `#33` and completed it after canonical validation.

Institutional-change checkpoint: added a separate model for reproduction,
critical junctures, layering, drift, conversion, displacement, and exhaustion.
Added `ChangeCase` passport, competing functionalist/power explanations, and
falsifiers for claims about why an institution arose or persisted. Added
historical-institutionalist sources and an explicit warning against stretching
the typology to every kind of change. Opened managed leaf `#36` under sub-epic
`#35` and completed it after canonical validation.

Mass-network checkpoint: added threshold and network-diffusion models for
collective behavior, including simple versus complex contagion, tipping points,
global cascades, bridges, and safe diagnostic procedures. Separated spread of
behavior from truth of a belief and added falsifiers for a network explanation.
Added four sources on threshold models, global cascades, complex contagions, and
empirical diffusion. Opened managed leaf `#38` under sub-epic `#37` and
completed it after canonical validation.

Power-and-legitimacy checkpoint: separated power, authority, legitimacy,
incentives, persuasion, and coercion. Added Weberian authority types,
French–Raven power sources, pragmatic/moral/cognitive legitimacy, a
Power/AuthorityCase passport, and falsifiers for the claim that compliance
equals belief. Added five sources from sociology, organizational research, and
social psychology. Opened managed leaf `#40` under sub-epic `#39` and completed
it after canonical validation.

Delegation checkpoint: added principal–agent analysis for adverse selection,
hidden action, hidden information, multitask distortion, multiple principals,
and formal versus real authority. Added cross-system comparisons for states,
corporations, and NRMs, plus a delegation passport and falsifiers for the claim
that more monitoring always improves execution. Added four delegation and
bureaucracy sources. Opened managed leaf `#42` under sub-epic `#41` and
completed it after canonical validation.

Collective-choice checkpoint: separated factual aggregation, preference
aggregation, and execution. Added Condorcet cycles, Arrow trade-offs,
correlated-vote limits on jury-theorem intuition, procedure selection by risk and
reversibility, a multidimensional DecisionQuality vector, and falsifiers for
the claim that majority rule directly expresses a group will. Added three
social-choice sources and an article unit. Opened managed leaf `#44` under
sub-epic `#43` and completed it after canonical validation.

Popular-reading checkpoint: turned the bibliography into a ten-stop route from
basic coordination to Sytrism design, with source order and expected learning
output. Added a repeatable article brief requiring a concrete scene, one
mechanism, one minimal formula, historical contrast, boundary case, practical
test, rights/exit check, and source triangulation. Opened managed leaf `#46`
under sub-epic `#45` and completed it after canonical validation.

Evidence-quality checkpoint: added source-type boundaries for conceptual,
formal, experimental, historical, comparative, review, and popular material.
Added `EvidenceCard` and rules separating correlation from causation,
persistence from fairness, capacity from good governance, cohesion from consent,
and diffusion from truth. Direct HTTP checking found 48 successful responses and
35 publisher/DOI `403` responses; the latter were treated as access defenses,
not broken links, and source records were verified through web retrieval where
available. Opened managed leaf `#48` and completed it after canonical validation.

Article-draft checkpoint: created first plain-language article draft
`how-govern-millions.md`. Used one mechanism engine — coordination — and
translated research into eight recurring tasks: visibility, free-rider control,
local knowledge, feedback, conflict, power constraints, shared expectations,
and adaptation/exit. Added a minimal formal model, historical contrasts,
corporate and mass-behavior sections, a protective NRM analysis, and an
evidence table with uncertainty boundaries. Linked draft from Wiki index and
source map. Opened managed leaf `#50` under article-drafts sub-epic `#49`;
completed after canonical validation, style audit, link/fence checks, and
reiteration audit.

Corporate-management article checkpoint: created
`why-firms-use-hierarchy.md` from fresh source review of Coase, Williamson,
Aghion–Tirole, and Holmström–Milgrom. Explained market/hierarchy/hybrid choice,
formal versus real authority, principal–agent failure, multitask measurement,
corporate culture, and a management-case passport with a pilot protocol. Kept
managerial control separate from information quality, autonomy, accountability,
and exit. Linked draft from Wiki index and source map. Opened managed leaf `#55`
under article-drafts sub-epic `#49`; completed after canonical validation,
style audit, link/fence checks, and reiteration audit.

Collective-choice article checkpoint: created `how-groups-decide.md` from
fresh review of Stanford Encyclopedia entries on social choice, Arrow’s theorem,
jury theorems, and network dynamics of social influence. Separated factual
aggregation, preference aggregation, procedure, delegation, execution, and
outcome. Added procedure-selection matrix, `DecisionQuality` vector, dissent
and reversibility checks, and a Sytrism design boundary. Opened managed leaf
`#57` under article-drafts sub-epic `#49`; completed after canonical
validation, style audit, link/fence checks, and reiteration audit.

Mass-behavior article checkpoint: created `why-masses-act-together.md` from
fresh source review of Granovetter threshold models, Watts global cascades,
Centola–Macy complex diffusion, and Centola’s online network experiment.
Explained heterogeneous thresholds, network structure, robust-yet-fragile
cascades, distinctions between information/emotion/action/identity/norm, and a
defensive `CascadeCase` passport. Explicitly separated diffusion from truth,
consent, fairness, and harm. Linked draft from Wiki index and source map.
Opened managed leaf `#56` under article-drafts sub-epic `#49`; completed after
canonical validation, style audit, link/fence checks, and reiteration audit.

State-formation article checkpoint: created `why-states-emerge.md` from fresh
source review of comparative early-state research, informational foundations of
tax capacity, Besley–Persson state capacity, and the bellicist debate. Used one
plain-language mechanism: the state as a stack of information, extraction,
security, execution, adjudication, legitimacy, succession, and feedback
contours. Added `StateCapacity` and `StateChange` passports and separated
capacity, governance quality, and outcomes. Linked draft from Wiki index and
source map. Opened managed leaf `#54` under article-drafts sub-epic `#49`;
completed after canonical validation, style audit, link/fence checks, and
reiteration audit.

NRM-case article checkpoint: created
`closed-groups-and-new-religious-movements.md` from fresh source review of
conversion and brainwashing debates, costly commitments, abuse in NRMs,
People’s Temple, Aum Shinrikyo, Shakers, Oneida, and Millerites. Compared
meaning, socialization, rituals, obligations, authority, resources,
information, sanctions, appeal, and exit. Added `NRMCase` and `ControlRisk`
passports and protective safeguards; kept group cohesion, voluntary entry,
belief, and actual harm analytically separate. Linked draft from Wiki index and
source map. Opened managed leaf `#53` under article-drafts sub-epic `#49`;
completed after canonical validation, style audit, link/fence checks, and
reiteration audit.

Culture-and-norms article checkpoint: created
`culture-as-coordination-infrastructure.md` from fresh source review of norm
dynamics, cultural group selection, cross-cultural cooperation, synchrony, and
the moralizing-gods debate. Formalized a norm passport, a religion-as-bundle
model, a corporate-culture observation table, a protective closed-group risk
vector, and a pilot procedure for changing norms. Kept cohesion separate from
fairness, consent, safety, and truth. Linked draft from Wiki index and source
map. Opened managed leaf `#51` under article-drafts sub-epic `#49`;
completed after canonical validation, style audit, link/fence checks, and
reiteration audit.

Series-index checkpoint: created `research/articles/index.md` as a single
reading route for seven governance drafts. Added common frame
`задача → механизм → институт → цена → результат → сбой → голос и выход`,
comparison table, organization-analysis card, scope boundaries, and next
editorial cycle. Linked index from Wiki navigation and source map. Opened
managed leaf `#58` under article-drafts sub-epic `#49`.

AI-governance article checkpoint: created
`research/articles/can-ai-become-governance-layer.md` from fresh source review
of NIST AI RMF, algorithmic management, model cards, dataset datasheets,
automation bias, human-in-the-loop experiments, reviewability, internal and
external audit, and lifecycle sources of harm. Formalized
`AI-Governance = (signals, model, authority, operator, feedback, appeal/exit)`,
bounded `Delegation`, provenance `Claim`, and an `AIQuality` vector. Separated
search, recommendation, sorting, gatekeeping, and execution; added a practical
pilot protocol, red-team checks, shutdown triggers, and Sytrism boundaries.
Linked article from Wiki index and article-series index/source map. Opened and
completed managed leaf `#60` after canonical validation, style audit,
link/fence checks, and source-map update.

Comparative-cases article checkpoint: created
`research/articles/comparative-governance-cases.md` from fresh review of
self-governing irrigation, Nepal community forestry, comparative state
formation, tax legibility, Chinese examination bureaucracy, firm boundaries,
peer governance, governance capture, and closed-group control. Applied one
`Case` card and one `Fit` function across commons, state, bureaucracy, firm,
network, and NRM examples. Separated capacity from outcomes, local knowledge
from scale, cohesion from consent, and decentralized governance from automatic
success. Linked article from Wiki index and article-series index/source map.
Opened and completed managed leaf `#61` after canonical validation, style audit,
link/fence checks, and source-map update.

Power-and-authority article checkpoint: created
`research/articles/power-authority-legitimacy.md` from fresh review of Weberian
authority, French–Raven power bases, legitimacy psychology, meta-analyses of
obedience, Mann's state power, organizational power, and exit/voice. Separated
capacity to force, recognized right to demand, and perceived legitimacy. Added
`PowerCase`, source-specific tests, state/firm/NRM/peer-network comparison,
historical layering, and a Sytrism design boundary. Linked article from Wiki
index and article-series index/source map; expanded central bibliography to 99
entries. Opened and completed managed leaf `#65` after canonical validation,
style audit, link/fence checks, and source-map update.

Kaneo queue checkpoint: created four planned follow-up tasks under article-drafts
sub-epic `#49`: AI as a governance layer (`#60`), comparative state/company/
decentralized cases (`#61`), publication revision of seven drafts (`#62`), and
a practical field guide for power, exit, appeal, and feedback (`#63`).
Opened managed leaf `#64` to record this queue in canonical Wiki log.

Collective-action article checkpoint: created
`research/articles/free-riding-and-cooperation.md` from fresh review of Olson,
Ostrom, Fehr–Gächter, and a preregistered cross-cultural meta-analysis. Separated
public goods, common-pool resources, and club goods; formalized `CollectiveAction`
and `CooperationQuality`; explained selective incentives, monitoring, sanctions,
reputation, norms, culture, scale, and exit. Added practical launch protocol and
Sytrism boundary, keeping contribution, cohesion, fairness, and consent distinct.
Linked article from Wiki index and article-series index/source map; expanded
central bibliography to 102 entries. Opened and completed managed leaf `#66`
after canonical validation, style audit, link/fence checks, and source-map update.

Practical field-guide checkpoint: created `research/governance-field-guide.md`
as a defensive, plain-language diagnostic for task fit, information flow, real
power, incentives, voice, exit, red-team testing, reversible pilots, and safety
boundaries. Linked it from Wiki index, article-series index, and source map.
Opened and completed managed leaf `#63` after canonical validation, style audit,
link/fence checks, and source-map update.

Publication-revision checkpoint: updated Kaneo task `#62` from seven to eleven
governance drafts and created `research/articles/publication-audit.md`. The audit
records one mechanism, one anchor case, one counterexample or boundary, one
reader-facing diagram, a practical test, source strength, and next edit for every
draft. Added explicit counterexample sections to all eleven articles and used
`how-govern-millions.md` as the first throughline revision with a city-bridge case
and a centralization failure case. Rechecked every article's evidence notes,
formulas, fenced blocks, local links, and placeholders. Current direct URL audit:
123 unique URLs, 75 `200`, 5 `203`, 43 `403`; no `404` or network-error results.
Opened and completed managed leaf `#62`; final publication remains a later
editorial stage because article status stays `draft` until human-facing copy edit.

Next-queue checkpoint: created planned Kaneo tasks under article-drafts
sub-epic `#49`: `#68` for publication-ready diagrams, Hugo metadata and
accessible cross-links; `#69` for a branching historical comparison from kinship
and city-states through empires, bureaucratic states, firms, platforms and closed
groups; and `#70` for operational measures of voice, appeal, exit and feedback.
These tasks turn current growth points into separate evidence loops rather than
one undifferentiated research backlog. Opened managed leaf `#67` to record the
queue; task statuses and parent relations were read back from Kaneo.

Governance-measures checkpoint: created
`research/articles/measuring-voice-appeal-exit-feedback.md` and added it as
article 12. The article operationalizes `VoiceAccess`, `VoiceResponse`,
`VoiceInfluence`, `CorrectionLatency`, `AppealQuality`, `RetaliationRisk` and
`ExitCost`; separates voice, appeal, exit and feedback; adds privacy safeguards,
false-positive interpretations, institution-specific cases and a reversible
pilot. Fresh sources include Morrison on employee voice and silence, Edmondson
on psychological safety, Colquitt on organizational-justice dimensions, UN
grievance-mechanism criteria, Gehlbach's formal exit/voice model, and empirical
work on retaliation costs. Updated article index, publication audit, source map,
Wiki navigation and central bibliography to 12 drafts and 107 source records.
Direct audit now covers 130 unique URLs: 75 `200`, 6 `203`, 49 `403`; no `404`
or network-error results. Opened and completed managed leaf `#70` after
canonical link, fence, heading, placeholder, style and source checks.

Historical-comparison checkpoint: created
`research/articles/governance-evolution-kinship-to-platforms.md` and added it as
article 13. The article compares kinship and local reputation, city-states,
imperial intermediaries, bureaucratic state capacity, firms, peer networks and
algorithmic platforms. It formalizes `InstitutionLayer` and `ChangeCase`, keeps
historical evolution as layering rather than a progress ladder, and separates
capacity, governance, outcome, power, feedback and exit. Fresh evidence records
108–114 cover Seshat cross-cultural complexity, evolutionary-driver models,
landesque capital, Mediterranean city-states, tax bureaucratization and platform
governance. Updated article index, publication audit, source map, Wiki navigation
and central bibliography to 13 drafts and 114 source records. Direct audit now
covers 138 unique URLs: 72 `200`, 7 `203`, 5 `302`, 53 `403` and one TLS network
error; no `404` result. Structural validation passed for 28 Markdown files,
13 article drafts, 13 counterexample headings, 114 source records and all local
links; new article style audit returned warnings only. Opened and completed
managed leaf `#69` after canonical validation and evidence checks.

Publication-kit checkpoint: created `research/publication-kit.md` with four
portable text-diagram specifications (`coordination-loop`, `institution-stack`,
`power-correction`, `historical-layering`), 13 article placements and alt-texts,
a stable Hugo front-matter template, metadata table with 13 `translationKey`
values, and a publication checklist. Kept diagrams text-first because repository
has no Mermaid or diagram shortcode; this preserves accessibility and avoids
inventing theme support. Linked kit from Wiki navigation, source map and
publication audit. Structural validation passed for 29 Markdown files, 13
article drafts, four diagram IDs, 114 source records and all local links; style
audit returned warnings only. Normal Hugo production build passed with 26 EN,
25 RU and 25 UK pages; one pre-existing raw-HTML warning remains, while
`--panicOnWarning` fails on that warning in the existing tag-page render.

Evidence-matrix checkpoint: created `research/evidence-matrix.md` with 15
claim cards covering commons, cooperation, voice, firms, state formation, war,
bureaucracy, culture, religion, legitimacy, mass cascades, NRM, collective
choice, platforms/AI and historical complexity. Each card separates mechanism,
evidence type, identification, counterexample, falsifier and transfer limit.
Added a plain-language evidence taxonomy (`A` repeatable mechanism, `B`
conditional connection, `C` case/framework), five cross-cutting falsifiers and
six evidence-backed backlog tasks with data requirements and close criteria.
Linked matrix from Wiki navigation and source map. Structural validation passed
for 30 Markdown files, 13 article drafts, 15 evidence cards, 114 source records
and all local links; style audit returned warnings only; `git diff --check` pass.

Culture-and-religion checkpoint: created `research/culture-religion-evidence.md`
as a plain-language supplement to the culture and NRM articles. It separates
four mechanisms — norm expectations, supernatural monitoring, costly commitment
and socioecological fit — from control risks, parochialism and stratification.
It records the Nature study of 591 participants in eight communities, the
counter-analysis of 822 supernatural explanations across 114 non-industrial
societies, and the phylogenetic analysis of 93 Austronesian cultures; each is
paired with causal and transfer limits. Added central source records 115–116,
linked dossier from Wiki navigation and source map, and ran direct URL audit.
Validation passed for 31 Markdown files, 13 article drafts, 116 source records,
all local links and `git diff --check`; style audit returned warnings only.
Current URL audit covers 140 unique URLs: 74 `200`, 7 `203`, 5 `302`, 53 `403`
and one TLS network error; no `404` result.

Regional-atlas checkpoint: created `research/historical-regional-cases.md` with
three comparable passports — later medieval Mediterranean city-states, early
modern European state-building, and imperial Chinese examination bureaucracy.
Each passport records initial constraint, scale, information, decision rights,
resource extraction, legitimacy, feedback, exit cost, counterexample, transfer
limit and unresolved causal question. Added a shared `HistoricalCase` schema,
four competing historical hypotheses and a function-preserving transfer table
for modern organizations and Sytrism. Linked atlas from Wiki navigation and
source map; reused existing source records to avoid duplicate bibliography
entries. Structural validation passed for 33 Markdown files, three regional
case sections, four regional evidence rows, 120 source records and all local
links; style audit returned warnings only; `git diff --check` pass.

Management-evidence checkpoint: created `research/management-evidence.md` as a
plain-language supplement to the corporate-management draft. It formalizes
`ManagementSystem`, `Delegation`, `AIManagement`, `ManagementAudit` and
`ManagerMandate`; separates monitoring, targets, incentives and delegation; and
uses the Indian management field experiment, its nine-year follow-up, World
Management Survey evidence and a systematic review of algorithmic management.
The dossier keeps productivity, quality, safety, worker voice and exit as
separate outcomes and adds a reversible company pilot. Added central source
records 117–120, linked dossier from Wiki navigation and source map. Structural
validation passed for 32 Markdown files, 13 article drafts, 120 source records
and all local links; style audit returned warnings only; `git diff --check` pass.
Current URL audit covers 144 unique URLs: 76 `200`, 7 `203`, 5 `302`, 55 `403`
and one TLS network error; no `404` result.

AI-governance evidence checkpoint: created
`research/ai-governance-evidence.md` as a plain-language supplement to the AI
article. The dossier separates model error, institutional error, authority
error, and feedback error; formalizes `AIGovernance`, `DelegationRisk`,
`ReviewQuality`, `AppealQuality`, `AuditCase`, and `ShutdownDesign`; compares
human-in-the-loop, human-on-the-loop, human-in-command, and human-on-appeal;
and adds a reversible pilot with baseline, provenance, override, appeal,
external-review, and shutdown checks. Added source 121 on human-on-appeal,
linked the dossier from Wiki navigation and source map. Opened managed leaf
`#76` for this evidence loop. Directly checked new source URL: `302` redirect;
repository-wide URL inventory is now 145 unique URLs with no `404` in the
current audit. Structural and style validation passed; Hugo verification is
recorded with the existing raw-HTML warning noted in earlier checkpoints.

AI-governance follow-up queue: created and linked six Kaneo tasks under article
sub-epic `#49`. P0 tasks are `#77` substantive appeal measurement, `#78`
comparison of human-on-appeal with universal human approval, and `#79`
shutdown/rollback/revocation testing. P1 tasks are `#80` internal versus
external audit comparison, `#81` effects on voice/retaliation/exit, and `#82`
independence of signals versus correlated AI error. Opened managed leaf `#83`
to record this queue in the canonical log.

AI-appeal evidence checkpoint: extended
`research/ai-governance-evidence.md` with a substantive-appeal measurement
section and `AppealPilot` protocol. The new section separates access,
registration, decision change, outcome improvement, reviewer independence,
correction latency, retaliation/cost, and learning loop. It adds a three-arm
low-stakes comparison (`B0` baseline, `B1` universal approval, `B2`
human-on-appeal) and explicitly records that direct empirical comparisons remain
limited. Added source records 122–123 covering a 100-participant conjoint study
of appeal preferences and a preregistered randomized experiment on experts'
direction-specific deference to AI-labelled errors. The current URL inventory
is 147 unique URLs: the new CHI DOI returned `302`, the new PMC record `200`,
and no `404` was observed. Managed leaf `#84` records this evidence loop; Kaneo
task `#77` remains the corresponding high-priority follow-up for a future
observed pilot.

AI-oversight counterevidence checkpoint: added sources 124–125 and a boundary
section to `research/ai-governance-evidence.md`. The new evidence distinguishes
perceived fairness from actual accuracy: a large hybrid-procedure experiment
found fairness-perception gains even with minimal human scrutiny, while a
separate experiment found that calibrated confidence cues improve reliance and
miscalibrated high confidence can amplify error. The current URL inventory is
149 unique URLs; both new DOI links returned `302`. Updated `AppealPilot` so its
primary outcomes include error direction, calibration, subgroup differences,
and legitimacy/accuracy separation. Managed leaf `#85` records this cycle.

AI-shutdown checkpoint: added sources 126–129 and extended
`research/ai-governance-evidence.md` with off-switch theory, shutdown
instructability, partial observability, and the runtime/harness boundary. The
working hypothesis is that a stop button inside the same authority contour is
not independent control: safety depends on external reachability, timing,
fail-safe behavior, reversibility, and preserved evidence. Added heuristic
`ShutdownSafety`, `ResidualHarm`, and `ActionGate` schemas plus a low-risk test
for external action gates, bounded irreversible actions, stop propagation,
rollback, restart integrity, network loss, prompt injection, stale approval and
duplicate delivery. The four new source URLs returned `200`; current inventory
is 153 unique URLs with 81 `200`, 7 `203`, 9 `302`, 55 `403` and one TLS network
error. Theoretical and policy sources are marked as non-causal limits, and the
counterexample separates stopping a process from revoking already-issued
external authority. Managed leaf `#86` records this cycle.

Human-oversight comparison checkpoint: opened managed leaf `#87` and added
sources 130–136 to the central bibliography and
`research/ai-governance-evidence.md`. The new evidence narrows the H1
hypothesis: selective prediction can partly reduce harm from inaccurate AI, but
deferral changes human behavior and can increase false negatives; class-
dependent miscalibration can make the deferral signal itself unreliable. A
randomized oncology workflow found Human+AI accuracy of 76.1% versus 71.5% for
Human-alone on 355 retrospective charts, without an efficiency gain, while a
separate randomized clinical vignette study found standard AI improved accuracy
but systematically biased AI reduced it. The current evidence still does not
directly compare universal human approval with human-on-appeal on the same
stream. Added `ReviewBurden`, `NetGovernanceValue`, `AppealCoverage`, and
`MissedRisk` as measurement heuristics, plus a falsifier for the claim that
appeal is cheaper without hidden harm. Seven new source URLs returned `200` or
redirect responses in the direct check; current inventory is 160 unique URLs
with 84 `200`, 8 `203`, 12 `302`, 55 `403` and one TLS network error. Managed
leaf `#87` records this cycle.

NRM-exit checkpoint: opened managed leaf `#88` and added sources 137–138 to the
central bibliography and the closed-groups article. The new section separates
formal exit from feasible exit over time: Coates' qualitative study of 23 former
members from 11 movements reports different exit trajectories, while Boeri's
ethnography shows gendered and post-exit costs without claiming a representative
rate for all former members. Added `ExitFeasibility` as a measurement heuristic
covering external ties, independent resources, identity alternatives,
information, safety, sanctions and dependency. The current inventory is 162
unique URLs with 84 `200`, 8 `203`, 13 `302`, 56 `403` and one TLS network error;
the two new source URLs returned `403` and `302` (the Wiley article was readable
through its publisher page despite direct-link protection). Managed leaf `#88` records this
cycle.

Editorial checkpoint: opened managed leaf `#91` and edited
`research/articles/why-states-emerge.md`. Added a plain six-step schema
`видимость → ресурс → аппарат → исполнение → ограничение → обратная связь` and
three regional passports: late-medieval Mediterranean city-states, early-modern
European state-building, and imperial Chinese examination bureaucracy. Each
passport separates the coordination problem, mechanism, and failure boundary;
the article now states that these are comparative routes, not a universal ladder.
Updated publication audit with the completed regional-case edit. Managed leaf
`#91` records this cycle.

Editorial checkpoint: opened managed leaf `#92` and edited
`research/articles/why-firms-use-hierarchy.md`. Added a five-question map for
choosing market, hierarchy, hybrid or network by location of knowledge,
measurability, error reversibility, real authority and exit. Added a concrete
call-center KPI example showing how a rising closure count can hide hard cases,
repeat work and suppressed risk signals. Updated publication audit with the
completed practical-management edit. Managed leaf `#92` records this cycle.

Editorial checkpoint: opened managed leaf `#90` and edited
`research/articles/culture-as-coordination-infrastructure.md`. Added a plain
four-question diagnostic in the opening: what a norm predicts, which collective
task it solves, who pays and benefits, and who can challenge or change it. This
keeps the article's central mechanism — norms reduce coordination uncertainty —
separate from the normative claim that a culture is fair or safe. Updated the
publication audit with the completed orientation edit. Managed leaf `#90`
records this cycle.

Editorial checkpoint: opened managed leaf `#89` and edited
`research/articles/how-govern-millions.md` as the first publication reference.
The opening now states the central claim in plain language, adds a compact
`задача → проверка` map for all eight coordination tasks, and explicitly routes
each later section back through the city-bridge case. The article keeps its
formal model, evidence notes, one counterexample and defensive treatment of
closed groups; publication audit now records the completed orientation edit and
the remaining length check. Managed leaf `#89` records this cycle.

Mass-behavior checkpoint: opened managed leaf `#94` and edited
`research/articles/why-masses-act-together.md`. Added a plain diagnostic that
separates seeing a signal, believing it, acting, and maintaining a norm; added
`CascadeMeasures` for visibility, belief, action, persistence, independent
confirmation, correction latency and exit cost. Added evidence from a
randomized village network intervention, partial-observability cascade
experiments, and a country-scale complex-contagion field experiment, with
explicit limits on ethical and contextual transfer. Updated publication audit
and central bibliography through source 141. Hugo production build passed with
EN 26, RU 25, UK 25; pre-existing raw-HTML warning on `/` remains. Managed
leaf `#94` records this cycle.

Power-and-authority checkpoint: opened managed leaf `#98` and edited
`research/articles/power-authority-legitimacy.md`. Added a concrete shift-manager
case showing that the same outward compliance can come from resource power,
authority of office, legitimate procedure, fear/dependence, or habit and norm.
Updated publication audit. Structural checks passed: 34 Markdown files, 13
article drafts, 141 sequential bibliography records, 165 unique URLs, no
missing local links, H1 or fence issues. Hugo production build passed with EN
26, RU 25, UK 25; pre-existing raw-HTML warning on `/` remains. Managed leaf
`#98` records this cycle.

Cooperation checkpoint: opened managed leaf `#100` and edited
`research/articles/free-riding-and-cooperation.md`. Added an opening map for
public good, common-pool resource and club good, plus five practical nodes for
cooperation: visible contribution, repeated interaction, proportional sanction,
appeal and exit. Added `SanctionQuality = evidence × proportionality × symmetry
× appeal` to show why monitoring alone does not make punishment legitimate.
Updated publication audit. Structural checks passed: 34 Markdown files, 13
article drafts, 141 sequential bibliography records, 165 unique URLs, no
missing local links, H1 or fence issues. Hugo production build passed with EN
26, RU 25, UK 25; pre-existing raw-HTML warning on `/` remains. Managed leaf
`#100` records this cycle.

Measurement checkpoint: opened managed leaf `#102` and edited
`research/articles/measuring-voice-appeal-exit-feedback.md`. Added an opening
chain `доступ → безопасность → содержательный ответ → влияние → исправление
правила → выход` and an explicit distinction between observing a measure and
rewarding a target. The article now warns that optimizing influence or low exit
rate can produce gaming and dependency. Updated publication audit. Structural
checks passed: 34 Markdown files, 13 article drafts, 141 sequential bibliography
records, 165 unique URLs, no missing local links, H1 or fence issues. Hugo
production build passed with EN 26, RU 25, UK 25; pre-existing raw-HTML warning
on `/` remains. Managed leaf `#102` records this cycle.

Collective-choice checkpoint: opened managed leaf `#96` and edited
`research/articles/how-groups-decide.md`. Added a plain decision map
`что произошло? → что считать важным? → кто выбирает? → кто исполняет?`, a
`DecisionCase` schema with review and exit fields, and a concrete hospital
example showing how a majority can aggregate one shared error. Updated
publication audit. Structural checks passed: 34 Markdown files, 13 article
drafts, 141 sequential bibliography records, 165 unique URLs, no missing local
links, H1 or fence issues. Hugo production build passed with EN 26, RU 25, UK
25; pre-existing raw-HTML warning on `/` remains. Managed leaf `#96` records
this cycle.

Comparative-governance checkpoint: opened managed leaf `#106` and edited
`research/articles/comparative-governance-cases.md`. Added a first-screen table
with three recurring contrasts: local versus distant knowledge, memory/rule
versus adaptation, and open cooperation versus dense commitment. Detailed cases
remain as an evidence appendix, with explicit fit and transfer boundaries.
Updated publication audit. Structural checks passed: 34 Markdown files, 13
article drafts, 141 sequential bibliography records, 165 unique URLs, no
missing local links, H1 or fence issues. Hugo production build passed with EN
26, RU 25, UK 25; pre-existing raw-HTML warning on `/` remains. Managed leaf
`#106` records this cycle.

AI-governance editorial checkpoint: opened managed leaf `#104` and edited
`research/articles/can-ai-become-governance-layer.md`. Added an opening ladder
`советник → gatekeeper → исполнитель` and a concrete test for what the system
can stop, deny, redistribute and who can reverse it. This makes the transition
from information aid to governance power explicit while retaining bounded
delegation, provenance, appeal, shutdown and exit safeguards. Updated
publication audit. Structural checks passed: 34 Markdown files, 13 article
drafts, 141 sequential bibliography records, 165 unique URLs, no missing local
links, H1 or fence issues. Hugo production build passed with EN 26, RU 25, UK
25; pre-existing raw-HTML warning on `/` remains. Managed leaf `#104` records
this cycle.

Institutional-evolution checkpoint: opened managed leaf `#108` and edited
`research/articles/governance-evolution-kinship-to-platforms.md`. Added a single
opening route from personal memory through archives, imperial intermediaries,
bureaucratic offices, firm contracts and algorithmic protocols. Added six
repeatable questions for each transition: which constraint fell, what became
visible, who gained authority, what became cheaper, what new failure appeared,
and who can correct or exit. Updated publication audit. Structural checks
passed: 34 Markdown files, 13 article drafts, 141 sequential bibliography
records, 165 unique URLs, no missing local links, H1 or fence issues. Hugo
production build passed with EN 26, RU 25, UK 25; pre-existing raw-HTML warning
on `/` remains. Managed leaf `#108` records this cycle.

NRM-audit checkpoint: opened managed leaf `#110` and synchronized
`research/articles/publication-audit.md` with completed NRM evidence. The row now
records the process of exit, `ExitFeasibility`, Coates and Boeri, and the need to
keep safety and sampling limits explicit before publication. Structural checks
passed: 34 Markdown files, 13 article drafts, 141 sequential bibliography
records, 165 unique URLs, no missing local links, H1 or fence issues. Hugo
production build passed with EN 26, RU 25, UK 25; pre-existing raw-HTML warning
on `/` remains. Managed leaf `#110` records this cycle.

AI-pilot checkpoint: opened managed leaf `#112` and expanded
`research/ai-governance-evidence.md` with an execution-ready `PilotCard`. The
protocol fixes a low-stakes reversible scope, excluded high-stakes decisions,
three arms (`B0`, `B1`, `B2`), assignment-level analysis, an independent
reference outcome, separate primary and safety outcomes, privacy boundaries and
stop-rules. It explicitly distinguishes perceived fairness from outcome quality
and keeps empirical tasks #77–#82 open until real data exist. Structural checks
passed: 34 Markdown files, 13 article drafts, 141 sequential bibliography
records, 165 unique URLs, no missing local links, H1 or fence issues. Hugo
production build passed with EN 26, RU 25, UK 25; pre-existing raw-HTML warning
on `/` remains. Managed leaf `#112` records this cycle.

Management-evidence checkpoint: opened managed leaf `#114` and expanded
`research/articles/why-firms-use-hierarchy.md` with a plain-language account of
Bloom et al.'s randomized Indian textile-factory experiment and its nine-year
follow-up. Added `ManagementIntervention` and `Durability` schemas, causal and
durability evidence notes, explicit transfer limits, and a practical warning
that management routines depend on information, delegation, training, manager
time, and continuity. Updated publication audit. Structural checks on changed
Markdown passed; `git diff --check` passed; Hugo production build passed with
EN 26, RU 25, UK 25; pre-existing raw-HTML warning on `/` remains. Managed
leaf `#114` records this cycle.

Mass-information checkpoint: opened managed leaf `#116` and added
`research/articles/how-mass-information-is-governed.md`. The new plain-language
article separates agenda-setting, framing, exposure, diffusion, belief, action,
correction, and exit; adds `InformationGovernance` and measurable channel
variables; and uses McCombs–Shaw, Entman, Bakshy–Messing–Adamic, and
Vosoughi–Roy–Aral with explicit limits on causal interpretation. Updated the
central bibliography to 145 sources, article index, source map, evidence matrix,
publication audit, and Wiki navigation. Structural checks passed: 35 Markdown
files, 14 article drafts, 145 sequential bibliography records, 0 H1 issues, 0
fence issues, and 0 missing local links. `git diff --check` passed; Hugo
production build passed with EN 26, RU 25, UK 25; pre-existing raw-HTML warning
on `/` remains. Managed leaf `#116` records this cycle.

Execution-gap checkpoint: opened managed leaf `#118` and added
`research/articles/why-rules-change-on-the-ground.md`. The article explains
bounded rationality and street-level discretion in plain language, separates
formal rule from local decision and outcome, adds an `Implementation` schema and
operational metrics, and balances Lipsky with evidence that work, peers, and
clients also shape bureaucratic effort. Updated article navigation, source map,
evidence matrix, publication audit, field-guide links, and central bibliography
to 149 sources. Structural checks passed: 36 Markdown files, 15 article drafts,
149 sequential bibliography records, 173 unique URLs, 0 H1 issues, 0 fence
issues, and 0 missing local links. `git diff --check` passed; Hugo production
build passed with EN 26, RU 25, UK 25; pre-existing raw-HTML warning on `/`
remains. Managed leaf `#118` records this cycle.

Reiterate correction checkpoint: opened managed leaf `#120` after independent
audit found that `ImplementationGap` mixed an intended outcome with observed
practice. Replaced it with a distance between formally specified rule and
observed practice, and added a warning that both must use one predeclared coding
scale before the gap is treated as numeric. Targeted metric checks and
`git diff --check` passed; full build and structural validation rerun after the
correction.

Leadership-succession checkpoint: opened managed leaf `#122` under sub-epic
`#121` and added `research/articles/how-leaders-emerge-and-change.md`. The new
plain-language article separates informal emergence, formal mandate, leader
effect, constraints, and succession; it adds a `LeadershipCase` schema,
operational metric names with a coding-scale caveat, a corporate succession
comparison, a counterexample, and safeguards for dissent, appeal, replacement,
and exit. Evidence is anchored in leader-emergence meta-analysis, Jones–Olken
national-leader transitions, upper-echelons theory, and CEO-succession studies.
Updated article index, publication audit, evidence matrix, source map, field-guide
links, Wiki navigation, and central bibliography to 154 sources. Structural
checks passed: 37 Wiki Markdown files, 16 article drafts, 154 sequential
bibliography records, 179 unique URLs, 0 H1 issues, 0 fence issues, and 0
missing local links. `git diff --check` passed. Hugo production build passed
with EN 26, RU 25, UK 25; pre-existing raw-HTML warning on `/` remains.

Institutional-learning checkpoint: opened managed leaf `#124` under sub-epic
`#123` and added `research/articles/how-institutions-learn-and-change.md`. The
new plain-language article separates data from learning, exploration from
exploitation, execution repair from rule revision, and genuine experiments from
political pilots. It adds a `LearningCase` schema, feedback/retention/rollback
metrics with a coding caveat, a historical path-dependence section, a safe pilot
design, a counterexample, and cross-links to culture, state, firms, closed groups,
and AI. Added March, policy-pilot, and NHS implementation evidence to the central
bibliography (158 sources total); updated article index, evidence matrix,
publication audit, source map, field-guide links, and Wiki navigation. Structural
checks passed: 38 Wiki Markdown files, 17 article drafts, 158 sequential
bibliography records, 183 unique URLs, 0 H1 issues, 0 fence issues, and 0
missing local links. `git diff --check` passed. Hugo production build passed
with EN 26, RU 25, UK 25; pre-existing raw-HTML warning on `/` remains.

Enforcement-legitimacy checkpoint: opened managed leaf `#126` under sub-epic
`#125` and added `research/articles/how-rules-become-binding.md`. The new
plain-language article separates compliance, fear, incentives, coordination, and
legitimacy; explains peer versus centralized monitoring; records the trust costs
of sanctions; and adds `EnforcementCase` with metrics for detection, selectivity,
appeal correction, retaliation, trust after sanction, and exit cost. It includes
practical safeguards: clear rules, accountable monitors, proportionate reactions,
independent appeal, time limits, and safe exit. Added four source records on
centralized sanctioning, trust and punishment, sanctioning/trust trade-offs, and
procedural legitimacy; central bibliography now has 162 sources. Updated article
index, evidence matrix, publication audit, source map, field-guide links, Wiki
navigation, and future article map. Structural checks passed: 39 Wiki Markdown
files, 18 article drafts, 162 sequential bibliography records, 187 unique URLs,
0 H1 issues, 0 fence issues, and 0 missing local links. `git diff --check`
passed. Hugo production build passed with EN 26, RU 25, UK 25; pre-existing
raw-HTML warning on `/` remains.

Log-integrity reiterate checkpoint: managed leaf `#129` audited the current
research log after detecting that the #126 and #124 entries had been appended in
the wrong chronological order. Reordered the two blocks so the recent sequence
is #122 leadership, #124 institutional learning, #126 enforcement and
legitimacy. Verified marker order directly with `rg`, reran Wiki structural
checks, and preserved unrelated `.tmp/` consultant materials.

Publication-kit coverage checkpoint: opened managed leaf `#131` under sub-epic
`#130` after reiterate found that `research/publication-kit.md` still described
only 13 drafts. Extended diagram mapping and Hugo metadata tables to all 18
articles, including mass information, execution gap, leadership and succession,
institutional learning, and enforcement and legitimacy. Coverage validation
passed: 18 diagram rows and 18 metadata rows, both numbered 1–18; all 18 article
filenames are present; no stale 13-draft markers remain. Wiki structural checks
remain green: 39 Markdown files, 18 article drafts, 162 sequential bibliography
records, 187 unique URLs, 0 H1 issues, 0 fence issues.

Wiki-health reiterate checkpoint: opened managed leaf `#133` after audit found
that `wiki-health.md` still described the 2026-08-30 inventory and omitted the
current research state. Refreshed it with 18 article drafts, 162 bibliography
records, 187 URLs, local-link and Markdown validation results, the pre-existing
Hugo warning, partial external-URL verification, and open empirical tasks
`#77–#82`. It now distinguishes verified local structure from unverified full
external availability and publication readiness.

External-URL audit checkpoint: opened managed leaf `#135` under sub-epic `#134`
and audited all 186 external link targets with direct `HEAD` requests. The
inventory also contains 187 URI-like strings because one public-site address in
`source-map.md` is inline code rather than a link target. Results were 103
`2xx`, 6 `302` DOI redirects, 76 `403` publisher/paywall/bot-protection
responses, and one OHCHR PDF TLS error in local PowerShell; the PDF loaded via
an alternate reader. No confirmed `404` or dead source was found. Updated
`wiki-health.md` and `research/articles/publication-audit.md` to separate link
reachability, access restrictions, redirect behavior, and source validity.

Access-restricted-source checkpoint: opened managed leaf `#137` under sub-epic
`#136` and ran a browser/metadata re-audit on five high-leverage sources from
records `#150–#155`. Northwestern author PDF confirmed Jones–Olken after OUP
redirect blocking; JSTOR metadata confirmed Hambrick–Mason while full text
remains unconfirmed; author-uploaded ResearchGate copy exposed Ensari et al.;
browser access to Wiley exposed Georgakakis–Ruigrok abstract and its 109-event
sample; browser and public PDF routes exposed March's abstract and model. The
audit supports a separate `access_route` variable: `HEAD 403` is not source
failure, and secondary copies are reading routes rather than independent
replication. Updated `research/articles/publication-audit.md`; kept #137 open
for sanctions, legitimacy, appeal, and closed-group sources.

Access-restricted-source follow-up: within open leaf `#137`, browser and
repository checks covered records `#159–#162`, `#54`, `#56`, and `#137`.
PNAS and an author PDF exposed the centralized-sanctioning experiment; Berkeley
author copy exposed the trust/punishment meta-analysis; ScienceDirect exposed
the sanctioning-paradox abstract; Birkbeck, OJP, and Oxford routes exposed
Jackson et al.'s legitimacy/compliance study. Oxford routes also exposed the
NRM conversion chapter and the costly-commitment article; Wiley exposed the
Coates disaffiliation abstract. Added an access-level distinction — publisher page,
author manuscript, institutional repository, abstract-only, or secondary host
— to the publication audit. Canonical bibliography links remain unchanged;
leaf `#137` stays open until the remaining access-restricted source families
are triaged.

Religion-and-ritual checkpoint: opened managed leaf `#139` under sub-epic `#138`
and added `research/articles/religion-ritual-and-group-coordination.md`. The
new plain-language draft isolates religious governance as a package of meaning,
norm, supernatural or social monitoring, ritual, costly commitment, boundary,
authority, appeal, and exit. It combines cross-cultural moralizing-gods evidence,
ritual synchrony and arousal experiments, costly-signaling review, and NRM exit
research. It adds `ReligiousGovernanceCase`, operational metrics with coding
caveats, a corporate comparison, a counterexample, and safeguards against
confusing cohesion with consent or using the text to design coercion. Added
source records `#163–#165` and synchronized article index, evidence matrix,
publication audit, publication kit, source map, Wiki navigation, field guide,
and future-series map. Structural checks passed: 40 Wiki Markdown files, 19
article drafts, 165 sequential bibliography records, 189 external URL targets,
0 H1 issues, 0 fence issues, 0 missing local links, and 0 article contract
issues. `git diff --check` passed; Hugo production build passed with EN 26,
RU 25, UK 25; pre-existing raw-HTML warning on `/` remains.

Comparative-state-formation checkpoint: opened managed leaf `#141` under
sub-epic `#140` after checking that the existing historical dossier covered
later city, military, and bureaucratic forms but lacked a reader-facing
comparison of primary state formation. Added
`research/articles/three-routes-to-state-formation.md` on Uruk, Qin, and the
Andes. The draft separates observation, interpretation, competing hypotheses,
and transfer limits; adds `StatePathCase`, a vector model of capacity, a modern
organization checklist, a counterexample, and evidence notes. Added source
records `#166–#171`, evidence claim `E22`, and synchronized article index,
publication audit, publication kit, source map, navigation, field guide,
Wiki-health, and future-series map. Current structural validation passed: 41
Wiki Markdown files, 20 article drafts, 171 sequential bibliography records,
40 publication-kit rows, 20 audit rows, 0 H1 issues, 0 fence issues, 0 missing
local links, and 0 article contract issues. The balanced Markdown parser found
193 unique external link targets and 194 raw URI-like strings. The prior full
HEAD audit remains documented as a separate snapshot; five new state-formation
targets were checked separately and returned four ordinary DOI `302` redirects
and one `403` access response. `git diff --check` passed; Hugo production build
passed with EN 26, RU 25, UK 25; pre-existing raw-HTML warning on `/` remains.

URL-audit reconciliation checkpoint: opened managed leaf `#142` under
sub-epic `#134` after the state-formation pass changed the external-link
inventory. Reparsed Markdown targets with balanced parentheses and found 193
unique external targets plus 194 raw URI-like strings (the extra string is the
public-site address repeated as inline code). A bounded parallel `curl -I`
snapshot with 24 workers and an 8-second timeout returned 74 `200`, 9 `203`,
3 `301`, 67 `302`, 6 `303`, 32 `403`, and two network errors; no `404` was
observed. The two errors were the OHCHR PDF and a Cambridge book URL. Updated
`wiki-health.md` and `research/articles/publication-audit.md` to record the
client, timeout, separate snapshots, and the boundary between reachability,
access route, and source validity. The source-research queue remains open for
remaining access-restricted routes and empirical tasks `#77–#82`.

Access-route follow-up: opened managed leaf `#143` under sub-epic `#136` and
re-audited four high-leverage access-restricted source families already used by
the article series. Gehlbach's SAGE page returned `403`, while the author's
24-page PDF opened; Gorwa–Binns–Katzenbach's SAGE page returned `403`, while
Oxford metadata and the HIIG PDF opened; Mokyr–Tabellini's Wiley route returned
`403` to local `HEAD`, while the official CESifo PDF and browser article page
opened; Bikhchandani et al.'s AEA route returned `403`, while the NBER record
and working-paper route opened. Updated `research/articles/publication-audit.md`
with `access_route` and `evidence_strength` kept separate. New route links were
checked: one DOI `302` and four institutional or author routes `200`. No source
was replaced because identity and content remained recoverable through a
traceable alternate route.

Corporate-and-political-institutions checkpoint: opened managed leaf `#145`
under sub-epic `#144` after the source-route review surfaced a direct bridge
between organizational design and state formation. Extended
`research/articles/why-firms-use-hierarchy.md` with a plain-language section on
clans, historical corporations, local public goods, countervailing powers,
membership, internal voice, and exit. Added `SocialOrganization` and evidence
claim `E23`, and updated the article publication audit. The source is kept as a
comparative historical synthesis and hypothesis, not a universal causal effect
or cultural ranking. Structural checks remain green: 20 article drafts, 171
bibliography records, 40 publication-kit rows, 20 audit rows, 0 H1 or contract
issues, and 0 local-link issues; `git diff --check` passed.

New-religious-movements checkpoint: opened managed leaf `#147` under sub-epic
`#146` and re-audited the NRM evidence layer through Cambridge open access,
Oxford metadata, Wiley/SAGE abstracts, OpenStax, and the SDSU Jonestown archive.
Corrected the article's case count from four to five, added Cambridge's explicit
warning that structural and cultural factors are not a predictive abuse checklist,
and added traceable primary-source and FBI-recovered-file routes for People’s
Temple. Added bibliography records `#172–#173`, evidence claim `E24`, and an
access-route table to the publication audit. New archive URLs returned `200` via
bounded `curl`; structural checks remain green: 20 article drafts, 173
bibliography records, 40 publication-kit rows, 20 audit rows, 0 H1 issues, 0
fence issues, 0 missing local links, and 0 article contract issues. Hugo
production build passed with EN 26, RU 25, UK 25; the pre-existing raw-HTML
warning on `/` remains. `git diff --check` passed.

Shutdown-control checkpoint: opened managed leaf `#158` under sub-epic `#157` and
extended `research/articles/can-ai-become-governance-layer.md` with the distinction
between pause, bypass, rollback, revoke, and decommission; a `ShutdownDesign`
passport; a test stream for time-to-pause, in-flight actions, evidence preservation,
fallback, restart gates, and independent stop authority; and a counterexample where
a working button fails to restore control. Added bibliography records `#184–#186`,
evidence claim `E29`, and a matching access-audit section using NIST, AWS, Australian
AI Centre, UNU, Japan AISI, and the off-switch model. AWS and AISI routes returned
`200`; the Australian page opened through web/browser access but local `curl` timed
out with `000`, which is recorded as a client-specific network failure. Structural
checks remain green: 20 article drafts, 186 sequential bibliography records, 216
Markdown URL targets, 217 raw URI-like strings, 40 publication-kit rows, 20 audit
rows, 0 H1 issues, 0 unmatched fences, 0 missing local links, and 0 article contract
issues. Hugo production build passed with EN 26, RU 25, UK 25; the pre-existing
raw-HTML warning on `/` remains. `git diff --check` passed.

External-audit checkpoint: opened managed leaf `#160` under sub-epic `#159` and
extended `research/articles/can-ai-become-governance-layer.md` with a parallel
internal/external audit protocol for content moderation. Added `AuditDisagreement`
fields for access, population, policy labels, metrics, harm definitions,
reproducibility, and authority to act; added a deployment decision rule where
missing access yields `unknown`, not `green`. Added bibliography records `#187–#190`,
evidence claim `E30`, and a matching access-audit section covering the DSA Research
API, EC researcher access, criterion audits, and the NYC audit regime. All four new
routes returned `200`. Structural checks remain green: 20 article drafts, 190
sequential bibliography records, 220 Markdown URL targets, 221 raw URI-like strings,
40 publication-kit rows, 20 audit rows, 0 H1 issues, 0 unmatched fences, 0 missing
local links, and 0 article contract issues. Hugo production build passed with EN 26,
RU 25, UK 25; the pre-existing raw-HTML warning on `/` remains. `git diff --check`
passed.

Ritual-and-religion checkpoint: opened managed leaf `#150` under sub-epic `#149`
and re-audited the governance role of religion and ritual through author or
institutional full-text routes for Purzycki et al., Lang et al., Lightner et al.,
Xygalatas et al., and Atwood et al. Extended
`research/articles/religion-ritual-and-group-coordination.md` with expectancy
controls, a measurement critique of the moralizing-high-god proxy, and explicit
separation of local prosocial effects from historical state-formation claims.
Added bibliography record `#174`, evidence claim `E25`, and a matching access
audit section. New PMC and Aarhus PDF routes returned `200`. Structural checks
remain green: 20 article drafts, 174 bibliography records, 40 publication-kit
rows, 20 audit rows, 0 H1 issues, 0 unmatched fences, 0 missing local links,
and 0 article contract issues. Hugo production build passed with EN 26, RU 25,
UK 25; the pre-existing raw-HTML warning on `/` remains. `git diff --check`
passed.

Mass-information checkpoint: opened managed leaf `#152` under sub-epic `#151`
and re-audited agenda-setting, framing, high-choice exposure, diffusion, and
correction. Extended `research/articles/how-mass-information-is-governed.md`
with the framing attitude/action gap, a correction-persistence mechanism, and
three evidence notes covering the Amsalem–Zoizner framing meta-analysis, the
Viewer's Choice experiment, and the Walter–Tukachinsky correction meta-analysis.
Added bibliography records `#175–#177`, evidence claim `E26`, and a matching
access-audit section. Cambridge framing route returned `200`; Viewer’s Choice
and Walter DOI routes returned `403` while metadata and abstracts remained
available. Structural checks remain green: 20 article drafts, 177 bibliography
records, 40 publication-kit rows, 20 audit rows, 0 H1 issues, 0 unmatched
fences, 0 missing local links, and 0 article contract issues. Hugo production
build passed with EN 26, RU 25, UK 25; the pre-existing raw-HTML warning on `/`
remains. `git diff --check` passed.

AI-appeal checkpoint: opened managed leaf `#154` under sub-epic `#153` and
tested the practical distinction between an appeal channel and a complaint box.
Extended `research/articles/can-ai-become-governance-layer.md` with an
`AppealBundle`, a reversible low-risk comment-queue pilot, access and
response-time measures, reviewer independence, `PowerToChange`, correction
latency, repeat-error tracking, and counterexample checks. Added bibliography
records `#178–#182`, evidence claim `E27`, and a matching access-audit section.
Official NIST, European Commission, Reddit, Appeals Centre Europe, and Oversight
Board routes returned `200` after correcting one stale Oversight Board slug.
Structural checks remain green: 20 article drafts, 182 bibliography records,
211 Markdown URL targets, 212 raw URI-like strings, 40 publication-kit rows, 20
audit rows, 0 H1 issues, 0 unmatched fences, 0 missing local links, and 0
article contract issues. Hugo production build passed with EN 26, RU 25, UK 25;
the pre-existing raw-HTML warning on `/` remains. `git diff --check` passed.

Human-oversight comparison checkpoint: opened managed leaf `#156` under
sub-epic `#155` and compared universal human approval with human-on-appeal or
selective deferral. Extended `research/articles/can-ai-become-governance-layer.md`
with `OversightComparison`, separate accuracy/latency/operator-load/automation-
bias/edge-case/override/experience measures, and evidence from selective-
prediction experiments with clinicians and conservation data plus a randomized
annotation study. Reused existing bibliography records `#130` and `#133`, added
record `#183`, and documented the alternative access routes in the publication
audit. The re-audit removed duplicate records rather than inflating the source
inventory. Structural checks remain green: 20 article drafts, 183 sequential
bibliography records, 213 Markdown URL targets, 214 raw URI-like strings, 40
publication-kit rows, 20 audit rows, 0 H1 issues, 0 unmatched fences, 0 missing
local links, and 0 article contract issues. New arXiv routes returned `200`.
Hugo production build passed with EN 26, RU 25, UK 25; the pre-existing raw-HTML
warning on `/` remains. `git diff --check` passed.

Algorithmic-management voice/exit checkpoint: opened managed leaf `#162` under
sub-epic `#161` and separated `voice_access`, `response`, `influence`,
`retaliation_risk`, `correction_latency`, `workload`, and `exit_cost` into a
`GovernanceChannels` audit. Extended
`research/articles/can-ai-become-governance-layer.md` and
`research/ai-governance-evidence.md` with Meituan voice evidence, Nigerian worker
interviews, OECD pre-deployment consultation, a manufacturing conflict case,
European workload/health evidence, and Lyft’s staggered natural experiment.
Added a baseline/control difference-in-differences protocol and recorded the
counterpoint that engagement, trust, or turnover intention are not substitutes
for safe voice, retaliation, correction, or realized exit. Added bibliography
records `#191–#198`, evidence claim `E31`, publication-audit routes, and updated
Wiki health counts. The current open corpus still has no single field rollout
with all requested channels; empirical Kaneo task `#81` remains open.
Structural checks pass: 20 article drafts, 198 sequential bibliography records,
228 Markdown URL targets, 229 raw URI-like strings, 40 publication-kit rows, 20
audit rows, 0 H1 issues, 0 unmatched fences, 0 missing local links, and 0
article contract issues. New OECD, PMC, Frontiers, arXiv, and DOI routes returned
`200`; MDPI, SAGE, and Wiley browser/metadata routes were confirmed after local
`403` responses. Hugo production build passed with EN 26, RU 25, UK 25; the
pre-existing raw-HTML warning on `/` remains. `git diff --check` passed.

Correlated-error checkpoint: opened managed leaf `#164` under sub-epic `#163` and
tested hypothesis that several AI checks add independent information only when
their evidence, retrieval, prompts, model families, labels and action gates are
not shared. Extended `research/articles/can-ai-become-governance-layer.md` and
`research/ai-governance-evidence.md` with `Signal`, `ErrorOverlap`, effective
vote intuition, a source/model/action diversity matrix, common-shock injection,
and a six-step empirical protocol. Added positive PoLL evidence, correlated LLM
errors, safety-judge artifacts, algorithmic monoculture, digital-pathology
source-shift, ensemble-diversity theory, and disagreement-penalized calibration.
Added bibliography records `#199–#208`, evidence claim `E32`, publication-audit
routes, and updated Wiki health counts. New routes all returned `200` through
local curl. This closes literature and design subtask #164; empirical Kaneo task
`#82` remains open for a real frozen-case/common-shock pilot.
Structural checks pass: 20 article drafts, 208 sequential bibliography records,
238 Markdown URL targets, 239 raw URI-like strings, 40 publication-kit rows, 20
audit rows, 0 H1 issues, 0 unmatched fences, 0 missing local links, and 0
article contract issues. Hugo production build passed with EN 26, RU 25, UK 25;
the pre-existing raw-HTML warning on `/` remains. `git diff --check` passed.

Public-data reanalysis checkpoint: opened managed leaf `#165` under sub-epic
`#163` and downloaded the authors’ reproducibility artifact
`data/final.csv` from `nikhgarg/llm_correlated_errors_public` into ignored
`.tmp/correlated-errors-20260831/`. Recomputed nine model score columns against
`Hand_firm_rate_comb`: 1,800 rows in the file, 450 human-labeled rows, and 443
complete panel cases. Strict score-mismatch proxy produced mean pairwise
binary-error phi `0.281`, heuristic `n_eff≈2.77` for nine nominal votes, mean
residual correlation `0.699`, and panel-median MAE `1.084` versus `1.034` for the
best single judge in this panel. SHA-256 of downloaded file is
`5277f91cfd9b656411e6f9c8c1a0ec9e622ab8d58bd53b5a4a64811c397b1ef7`.
Documented the calculation, proxy limits, and source in the AI article, evidence
dossier, matrix, bibliography, publication audit, and Wiki health. This closes
observational part of #82; source-diverse route and controlled common-shock
injection remain open. Structural checks pass: 20 article drafts, 209 sequential
bibliography records, 239 Markdown URL targets, 240 raw URI-like strings, 40
publication-kit rows, 20 audit rows, 0 H1 issues, 0 unmatched fences, 0 missing
local links, and 0 article contract issues. `git diff --check` passed.

Prompt-diversity checkpoint: opened managed leaf `#166` under sub-epic `#163` and
recomputed paired prompt variants in the same public `final.csv`. Across 22 model
prompt pairs, mean phi-correlation of binary score errors was `0.259`, with range
`-0.061…0.872`; this is prompt diversity only, not source diversity. Added the
result and a three-arm common-shock design (`B0` normal route, `B1` shared wrong
packet, `B2` independent evidence packet) to the AI article, evidence dossier,
and publication audit. B1/B2 have not been run against a live multi-route panel,
so Kaneo task `#82` remains open. Structural checks remain green: 20 article
drafts, 209 sequential bibliography records, 239 Markdown URL targets, 240 raw
URI-like strings, 0 H1 issues, 0 unmatched fences, 0 missing local links, and 0
article contract issues. `git diff --check` passed.

Reader-facing editorial checkpoint: opened managed leaf `#168` under sub-epic
`#167` and edited first article `how-govern-millions.md`. Reframed reader promise,
defined `Институт` as a practical rather than legal term, replaced unexplained
jargon, split dense sentences, renamed application section, and replaced repeated
summary with an operational closing checklist. Formal models, evidence links,
counterexamples, uncertainty, and limits were preserved. Popular-science style
audit reports no repeated sentence starts; remaining long-sentence notices are
table, source-heavy, and code-block diagnostics. Full structural audit remains
green: 20 article drafts, 209 sequential bibliography records, 239 Markdown URL
targets, 240 raw URI-like strings, 0 H1 issues, 0 unmatched fences, 0 missing
local links, and 0 article contract issues. Hugo production build passed with
EN 26, RU 25, UK 25; pre-existing raw-HTML warning on `/` remains.

Appeals-protocol checkpoint: opened managed leaf `#171` under sub-epic `#170`
for Kaneo task `#77`. Added `ai-appeals-pilot.md` with a low-stakes `A0/A1`
comparison, `AppealBundle`, gold-standard labels, reviewer independence,
`PowerToChange` seeded-case test, correction and latency metrics, repeated-
complaint learning loop, stop-rules, counterexamples, and transfer limits. Added
evidence claim `E33`, bibliography records `#210–#214`, source-audit rows, and
links from the AI dossier and Wiki index. The note is a protocol, not pilot data;
Kaneo #77 remains open until real cases are collected. Structural audit passes:
42 Wiki Markdown files, 20 article drafts, 214 sequential bibliography records,
242 unique Markdown URL targets, 243 unique raw URI targets, 0 H1 issues,
0 unmatched fences, 0 missing local links, and 0 article contract issues.
`git diff --check` passed; Hugo production build passed with EN 26, RU 25, UK 25;
pre-existing raw-HTML warning on `/` remains.

Culture editorial checkpoint: opened managed leaf `#172` under sub-epic `#167`
and edited `culture-as-coordination-infrastructure.md`. Reframed reader promise,
defined `референтная группа`, translated unnecessary English labels, separated
coordination from legitimacy and moral truth, and made the ritual, religion,
corporate-culture, and closed-group mechanisms easier to distinguish. Evidence,
counterexample, formulas, uncertainty, and transfer limits were preserved. Style
audit leaves only a numbered-list diagnostic and table diagnostics; full structural
audit passes: 42 Wiki Markdown files, 20 article drafts, 214 sequential
bibliography records, 242 unique Markdown URL targets, 243 unique raw URI targets,
0 H1 issues, 0 unmatched fences, 0 missing local links, and 0 article contract
issues. `git diff --check` passed; Hugo production build passed with EN 26, RU 25,
UK 25; pre-existing raw-HTML warning on `/` remains.

Culture article editorial checkpoint: opened managed leaf `#172` under sub-epic
`#167` and edited `culture-as-coordination-infrastructure.md`. Reframed reader
promise, defined `референтная группа`, removed unnecessary English labels, split
dense evidence-bearing sentences, and kept coordination, legitimacy, ritual,
religion, corporate culture, and closed-group control as distinct mechanisms.
Formulas, evidence notes, counterexample, uncertainty, and transfer limits were
preserved. Updated publication audit, article index, and Wiki health; two of 20
drafts now have reader-facing passes and 18 remain. Style audit has only
non-blocking numbered-list/table diagnostics. Structural audit passes: 42 Wiki
Markdown files, 20 article drafts, 214 sequential bibliography records, 242
unique Markdown URL targets, 243 unique raw URI targets, 0 H1 issues, 0 unmatched
fences, 0 missing local links, and 0 article contract issues. `git diff --check`
passed; Hugo production build passed with EN 26, RU 25, UK 25; pre-existing
raw-HTML warning on `/` remains.

Closed-groups article editorial checkpoint: opened managed leaf `#173` under
sub-epic `#167` and edited `closed-groups-and-new-religious-movements.md`.
Reframed reader promise, translated unnecessary English labels, defined costly
commitments in plain language, clarified `ExitFeasibility` inputs and renamed the
historical-table failure column to a practical diagnostic. Preserved the neutral
NRM frame, historical distinctions, evidence notes, counterexample, uncertainty,
and transfer limits. Updated publication audit, article index, and Wiki health;
three of 20 drafts now have reader-facing passes and 17 remain. Style, structural,
link, and Hugo checks passed; the pre-existing raw-HTML warning on `/` remains a
known non-blocking issue.

State-formation article editorial checkpoint: opened managed leaf `#174` under
sub-epic `#167` and edited `why-states-emerge.md`. Reframed reader promise,
translated `bellicist`, `state capacity`, and `cross-national` wording, replaced
technical «легитимация» with recognition of order, and explained `StateCapacity`
as a map of capabilities rather than a quality score. Preserved comparative
routes, evidence notes, counterexample, formal passports, uncertainty, and the
capacity/governance/outcome distinction. Updated publication audit, article
index, and Wiki health; four of 20 drafts now have reader-facing passes and 16
remain. Final style, structural, link, and Hugo checks are pending.

Corporate-hierarchy article editorial checkpoint: opened managed leaf `#175`
under sub-epic `#167` and edited `why-firms-use-hierarchy.md`. Reframed reader
promise, defined transaction costs, translated principal–agent, proxy and
open-source wording, and clarified formal versus real authority. Preserved
Coase/Williamson mechanisms, field-experiment evidence, management passport,
counterexample, uncertainty, and limits on historical transfer. Updated
publication audit, article index, and Wiki health; five of 20 drafts now have
reader-facing passes and 15 remain. Final style, structural, link, and Hugo
checks are pending.

Mass-action article editorial checkpoint: opened managed leaf `#176` under
sub-epic `#167` and edited `why-masses-act-together.md`. Reframed reader promise,
clarified threshold, simple/complex diffusion, spillover and counter-cascade
terms, and strengthened the separation between reach, belief, action, persistence
and truth. Preserved threshold/network models, field evidence, `CascadeCase`,
protective boundaries, counterexample, uncertainty, and safe-use limits. Updated
publication audit, article index, and Wiki health; six of 20 drafts now have
reader-facing passes and 14 remain. Final style, structural, link, and Hugo
checks are pending.

Collective-choice article editorial checkpoint: opened managed leaf `#177`
under sub-epic `#167` and edited `how-groups-decide.md`. Reframed reader promise,
translated dissent memo, approval voting and sunset clause, clarified legitimacy,
and made minority voice, independent information, delegation and cancellation
more operational. Preserved Arrow and jury-theorem limits, DecisionCase and
DecisionQuality, counterexample, uncertainty, and Sytrism transfer boundaries.
Updated publication audit, article index, and Wiki health; seven of 20 drafts now
have reader-facing passes and 13 remain. Final style, structural, link, and Hugo
checks are pending.

Power-and-authority article editorial checkpoint: opened managed leaf `#178`
under sub-epic `#167` and edited `power-authority-legitimacy.md`. Added a reader
promise, defined `Power`, `Authority` and `Legitimacy` in plain language, translated
KPI and peer-network terms, and made authority, appeal, exit and real information
control observable. Preserved Weber, French–Raven, obedience evidence, PowerCase,
counterexample, uncertainty, and state/firm/NRM transfer limits. Updated
publication audit, article index, and Wiki health; eight of 20 drafts now have
reader-facing passes and 12 remain. Final style, structural, link, and Hugo
checks are pending.

Comparative-governance article editorial checkpoint: opened managed leaf `#179`
under sub-epic `#167` and edited `comparative-governance-cases.md`. Added a reader
promise, translated trade-off, commons, review, fork, sunset and principal–agent
terms, and clarified why institutional mechanisms cannot be transferred without
checking scale, knowledge, monitoring, sanctions, appeal and exit. Preserved
historical contrasts, case/fit frame, evidence notes, counterexample, uncertainty,
and Sytrism design boundaries. Updated publication audit, article index, and Wiki
health; nine of 20 drafts now have reader-facing passes and 11 remain. Final
style, structural, link, and Hugo checks are pending.

Mass-information article editorial checkpoint: opened managed leaf `#180` under
sub-epic `#167` and edited `how-mass-information-is-governed.md`. Reframed reader
promise, translated agenda-setting, framing, exposure, diffusion and correction,
and clarified the difference between reach, attention, belief, action, persistence
and truth. Preserved communication theory, platform and field evidence,
`InformationGovernance`, correction limits, counterexample, uncertainty, and
anti-manipulation boundaries. Updated publication audit, article index, and Wiki
health; ten of 20 drafts now have reader-facing passes and 10 remain. Final style,
structural, link, and Hugo checks are pending.

Empirical-appeal checkpoint: opened managed leaf `#182` under the AI-governance
sub-epic and searched for external evidence that connects appeal to concrete
correction and institutional learning. Added three sources to the central
bibliography: a randomized expert-oversight experiment, a quasi-random study of
appeal reversals changing later judicial decisions, and an operational ML triage
study of hospital-denial appeals. Added evidence card `E34` and extended the AI
dossier with the distinction between `case_correction`, `upstream_feedback`,
`downstream_action`, `rule_or_model_update`, and `overreaction_check`. The search
did not find a direct external equivalent of the low-stakes `A0/A1` comparison;
task #77 therefore remains open. Structural and build validation are pending.

AI-governance article editorial checkpoint: opened managed leaf `#184` under the
article/AI-governance sub-epic and edited `can-ai-become-governance-layer.md`.
Added a reader-facing promise, translated gatekeeper and human oversight terms,
replaced the metaphor of a rubber stamp with the clearer idea of a decorative
signature, and added a table separating `case_correction`, `upstream_feedback`,
`downstream_action`, and `rule_or_model_update`. Linked new empirical evidence on
expert correction and appeal feedback without treating reversal as proof of
accuracy or institutional learning. Updated publication audit and Wiki health;
eleven of 20 drafts now have reader-facing passes and 9 remain. Structural, style,
link, and Hugo checks are pending.

Collective-action article editorial checkpoint: opened managed leaf `#186` under
the article/collective-action sub-epic and edited `free-riding-and-cooperation.md`.
Added a reader-facing promise, made the route from common good to appeal and exit
explicit, and separated cooperation from obedience by fear or lack of alternatives.
Added a practical test for whether an appeal changes sanctions, benefit distribution,
or exit cost. Updated publication audit and Wiki health; twelve of 20 drafts now
have reader-facing passes and 8 remain. Structural, style, link, and Hugo checks
are pending.

Governance-channels article editorial checkpoint: opened managed leaf `#188` under
the article/governance-channels sub-epic and edited
`measuring-voice-appeal-exit-feedback.md`. Added a reader-facing promise, clarified
that voice, appeal, feedback, and exit are connected but non-substitutable links,
and replaced the idea of a single governance score with a profile of breaks in the
chain. Updated publication audit and Wiki health; thirteen of 20 drafts now have
reader-facing passes and 7 remain. Structural, style, link, and Hugo checks are
pending.

Institutional-evolution article editorial checkpoint: opened managed leaf `#190`
under the institutional-evolution sub-epic and edited
`governance-evolution-kinship-to-platforms.md`. Added a reader-facing comparison
frame that separates institutional layers from a ladder of progress and translated
legibility as visibility for the governing apparatus. Updated publication audit and
Wiki health; fourteen of 20 drafts now have reader-facing passes and 6 remain.
Structural, style, link, and Hugo checks are pending.

Street-level-governance article editorial checkpoint: opened managed leaf `#192`
under the street-level-governance sub-epic and edited
`why-rules-change-on-the-ground.md`. Added a plain-language thesis that local
discretion is useful only when bounded, explained, and reviewable; hidden freedom
without appeal is arbitrary power. Updated publication audit and Wiki health;
fifteen of 20 drafts now have reader-facing passes and 5 remain. Structural, style,
link, and Hugo checks are pending.

Leadership article editorial checkpoint: opened managed leaf `#194` under the
leadership-governance sub-epic and edited `how-leaders-emerge-and-change.md`.
Added a reader-facing anchor that treats leadership as a temporary coordination
node rather than a magical personal trait, and tied usefulness to constraints,
replaceability, and participant exit cost. Updated publication audit and Wiki
health; sixteen of 20 drafts now have reader-facing passes and 4 remain.
Structural, style, link, and Hugo checks are pending.

Institutional-learning article editorial checkpoint: opened managed leaf `#196`
under the institutional-learning sub-epic and edited
`how-institutions-learn-and-change.md`. Added a reader-facing test that separates
signal, changed behavior, final outcome, and institutional memory, and connected
the Norwegian appeal-feedback study to the risk of overreaction. Updated publication
audit and Wiki health; seventeen of 20 drafts now have reader-facing passes and 3
remain. Structural, style, link, and Hugo checks are pending.

Enforcement article editorial checkpoint: opened managed leaf `#198` under the
enforcement-governance sub-epic and edited `how-rules-become-binding.md`. Added a
plain-language test for why people comply and translated public-goods and altruistic
punishment into accessible Russian. Preserved the distinctions among fear, incentive,
coordination, legitimacy, sanction, appeal, and exit. Updated publication audit and
Wiki health; eighteen of 20 drafts now have reader-facing passes and 2 remain.
Structural, style, link, and Hugo checks are pending.

Religion-and-ritual article editorial checkpoint: opened managed leaf `#200` under
the religion-governance sub-epic and edited
`religion-ritual-and-group-coordination.md`. Added a plain-language test that
distinguishes internal cooperation from outgroup treatment, minority safety,
disagreement, and exit cost. This keeps religious and secular practices comparable
by mechanism without reducing religion to control. Updated publication audit and
Wiki health; nineteen of 20 drafts now have reader-facing passes and 1 remains.
Structural, style, link, and Hugo checks are pending.

State-formation article editorial checkpoint: opened managed leaf `#202` under the
governance-research-publication-editing sub-epic and edited
`three-routes-to-state-formation.md`. Added a plain-language reading path that
applies the same four questions to each historical route — constraint, visibility,
mediator, and correction — and connects the comparison to companies, ministries,
and platforms. Updated publication audit and Wiki health; all twenty drafts now
have reader-facing passes. Structural, style, link, and Hugo checks are pending.

Reiterate knowledge audit checkpoint: opened managed leaf `#204` under the
governance-research-knowledge-audit sub-epic and reconciled stale entries in
`open-questions.md`. The document now distinguishes unresolved work — older-post
source notes, empirical validation for Kaneo tasks `#77–#82`, and final publication
triage — from the resolved dedicated Kaneo project binding and the completed
research evidence layer. Final structural and Hugo checks are recorded on the
managed leaf.

AppealMod evidence checkpoint: opened managed leaf `#206` under the
ai-governance-empirical-evidence sub-epic and added source `#218`, evidence row
`E35`, and corresponding entries in the AI-governance dossier and article audit.
The randomized Reddit field experiment is recorded as evidence about workload
and selection under appeal friction, not as proof of factual correction,
fairness, or safe access. Validation is pending.

Community-appeals checkpoint: opened managed leaf `#207` under the
ai-governance-empirical-evidence sub-epic and added source `#219`, evidence row
`E36`, and corresponding entries in the AI-governance dossier and article audit.
The Discord study adds a middle governance layer — local norms, collective review,
and rehabilitation — while remaining qualitative and selected; it does not close
the causal redress or safe-exit questions in #77–#82. Validation is pending.

Roadmap reconciliation checkpoint: opened managed leaf `#208` under the
governance-research-knowledge-audit sub-epic and clarified that the former future
article list is now a coverage map and next-cycle backlog. Linked the completed
20-draft reader-facing series to its canonical index and marked the standalone
institution-passport topic as an additional practical artifact. Validation is
pending.

Closed-groups exit checkpoint: opened managed leaf `#210` under the
closed-groups-religion-and-exit sub-epic and added source `#220`, evidence row
`E37`, and a source note to the NRM article and publication audit. The six-person
qualitative study is used to distinguish social exit costs and agency from claims
about universal coercion or prevalence. Validation is pending.

Discipline-map checkpoint: opened managed leaf `#212` under the
governance-research-discipline-map sub-epic and added cybernetics and
public-administration implementation as explicit bridge disciplines. Added
sources `#221–#222` and evidence rows `E38–E39`. The new layer distinguishes
nested feedback and requisite variety from a claim that polycentricity is always
better, and frontline discretion from a claim that formal rules disappear.
Validation is pending.

Metadata-reconciliation checkpoint: opened managed leaf `#213` under the
governance-research-knowledge-audit sub-epic and synchronized current counts in
`open-questions.md`, `publication-audit.md`, and `wiki-health.md`: 222 bibliography
records, 250 unique Markdown external URL targets, and 251 raw URI-like targets.
The one-count difference is the public-site address recorded as inline code.
Validation is pending.

Practical-design checkpoint: opened managed leaf `#215` under the
governance-research-practical-design sub-epic and connected the new discipline
layer to the field guide and growth map. Added `FeedbackLoop`, `ImplementationGap`,
`DiscretionRange`, `CorrectionLatencyByLayer`, and `CaseVariation`; the diagnostic
now compares formal rules with frontline practice and checks whether local signals
reach the rule owner. Validation is pending.

Reader-facing feedback checkpoint: opened managed leaf `#216` under the
governance-research-publication-editing sub-epic and added a short cybernetic
bridge to `why-rules-change-on-the-ground.md`. The article now connects frontline
discretion to observation delay, correction authority, institutional memory, and
nested rules, while preserving the limit that polycentricity is not automatically
better. Validation is pending.

NRM exit-cost checkpoint: opened managed leaf `#217` under the
closed-groups-religion-and-exit sub-epic and added conceptual source `#223` and
exploratory preprint `#224`. Extended the NRM article and evidence matrix to
separate expected penalties, post-exit identity/social costs, and mental-health
associations; the preprint is explicitly marked as not peer-reviewed and neither
source is used as a universal causal claim. Validation is pending.

NRM recovery checkpoint: opened managed leaf `#218` under the
closed-groups-religion-and-exit sub-epic and added qualitative source `#225` and
evidence row `E42`. The article now includes a recovery trajectory as a
counterbalance to exit costs, while preserving limits on self-selected samples,
causality and transfer across groups. Validation is pending.

Social-movement checkpoint: opened managed leaf `#220` under the mass-movements-
and-contentious-politics sub-epic and added a distinct discipline layer for
resource mobilization, dynamic contention, political opportunities and framing.
Added sources `#226`–`#229`, evidence row `E43`, a `MovementCapacity` heuristic,
`MovementCase` diagnostic, a growth point distinguishing cascades from durable
movements, and reader-facing material in `why-masses-act-together.md`.
Validation is pending.

Social-movement integration checkpoint: opened managed leaf `#221` under the
mass-movements-and-contentious-politics sub-epic and added the same mechanism to
the main mass-behavior section. The map now distinguishes threshold crossing,
coordinated action, resource/role retention, strategy under opposition,
demobilization, and institutional change; it also records the temporal test for
durable organization versus a short cascade or coalition. Validation is pending.

Countermovement checkpoint: opened managed leaf `#222` with a separate response
contour for state restriction, concession, negotiation and countermovement.
Added sources `#230–#231` and evidence row `E44`; the map and article now treat
response sequence as part of movement dynamics while preserving rights safeguards
and the boundary against targeting or evasion guidance. Validation is pending.

Public-choice checkpoint: opened managed leaf `#224` under the state-capacity-and-
political-economy sub-epic and added a constraint separating state capacity from
institutional capture. Added sources `#232–#233`, evidence row `E45`,
`CaptureCase`, a `CaptureRisk` heuristic, a state-formation article section and
publication-audit coverage. The new material treats public choice as a lens on
agents, incentives, concentrated benefits and diffuse costs, not as a universal
claim that public institutions cannot serve broad goals. Validation is pending.

Navigation checkpoint: opened managed leaf `#226` under the research-navigation-
and-knowledge-routing sub-epic and synchronized the article-series index with
the new state-capture and mass-movement response constructs. The route now points
readers from `StateCapacity` to `CaptureCase`, and from `CascadeCase` to
`MovementCase` and `ResponseCase`. Validation is pending.

Representation checkpoint: opened managed leaf `#228` under the intermediary-
institutions-and-representation sub-epic. Added sources `#234–#240` on parties,
interest groups, civil-society representation, delegation, candidate selection,
internal democracy and whistleblowing. Added `RepresentationCase` and
`GatekeeperRisk` to the main map, state-formation article, field guide, evidence
matrix and publication audit. The package now distinguishes internal
participation, external responsiveness, discipline, selection power and exit;
validation passed: `git diff --check`, Markdown structure/link audit and
`hugo --minify` all passed; pre-existing raw-HTML warning remains.

Historical-representation checkpoint: opened managed leaf `#230` under the
origins-and-evolution-of-representative-institutions sub-epic. Added sources
`#241–#246` on medieval representation, succession, the Crown of Aragon,
German cities, the coordination model of early assemblies and late-Qing local
councils. Added
`RepresentationEvolutionCase` and a three-phase distinction — coordination,
resource exchange, durable constraint — to the main map, state-formation article,
evidence matrix and publication audit. The historical section now separates
institutional origin from later function and consolidation; validation passed:
`git diff --check`, Markdown structure/link audit and `hugo --minify` all passed;
pre-existing raw-HTML warning remains.

Late-Qing supplement checkpoint: opened managed leaf `#231` under the same
origins-and-evolution-of-representative-institutions sub-epic after a completed
leaf could not be rewritten with later evidence. Added Thompson’s late-Qing
local-council case as a bounded cross-regional contrast and synchronized the
246-record bibliography, evidence boundary and health counts. Validation passed:
`git diff --check`, Markdown structure/link audit and `hugo --minify` all passed;
pre-existing raw-HTML warning remains.

AI-oversight empirical checkpoint: opened managed leaf `#233` under the
AI-governance-appeals-and-human-oversight sub-epic. Added sources `#247–#253`
covering cognitive forcing, inherited AI bias, a public-sector randomized trial,
selective adherence, human-centred appeal preferences, timing-based de-anchoring
and repeated-exposure estimands. The dossier now treats timing, trigger,
`PowerToChange`, subgroup outcomes and repeated exposure as separate fields;
validation is pending until this leaf is finished.

Administrative-learning checkpoint: opened managed leaf `#235` under the
appeals-administrative-error-and-institutional-learning sub-epic. Added sources
`#254–#259` on algorithmic public-benefits failures, Robodebt, appeal selection,
Universal Credit burden, and task-specific SNAP automation. Added
`AdministrativeErrorCase` and `SystemLearning`, separating case reversal from
aggregation, escalation, authority to pause or change, and verified downstream
repair. Validation passed: 42 Markdown files, 20 article drafts, 259 sequential
bibliography records, 287 unique Markdown external URLs, 288 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing `warning-goldmark-raw-html`
on `/` remains; real B1/B2 pilot #77–#82 remains open.

Culture-and-ritual checkpoint: opened managed leaf `#237` under the
ritual-religion-moral-norms-and-intergroup-boundaries sub-epic. Added sources
`#260–#265` on cultural-group selection, ritual synchrony trade-offs, Tiwanaku
state consolidation, denominational authority, control of dyadic ties, and
religious coercive-control safeguards. Added `NormGovernance`, `RitualTradeoff`
and `TieControl` and synchronized the culture/religion dossiers, two article
drafts, field guide and evidence matrix. Validation passed: 42 Markdown files,
20 article drafts, 265 sequential bibliography records, 293 unique Markdown
external URLs, 294 raw URI-like targets, 0 H1 issues, 0 unmatched fences,
0 missing local links, 0 article contract issues, `git diff --check`, and
`hugo --minify` with EN 26/RU 25/UK 25 pages and 64 static files per language.
Pre-existing `warning-goldmark-raw-html` on `/` remains; matching cases and
prospective data are still needed to validate the new diagnostic constructs.

Management-durability checkpoint: opened managed leaf `#239` under the
management-interventions-and-institutional-durability sub-epic. Added source
`#266` on a preregistered participatory-meeting field experiment in Chinese
factory groups, corrected source `#154` metadata for the CEO-succession
meta-analysis, and synchronized the management dossier, firm article, leader
article and evidence matrix. Added `ManagementDurabilityCase`, separating
initial effect, practice retention, worker voice, key-person dependence, leader
turnover and outcome after stop. Validation passed: 42 Markdown files, 20
article drafts, 266 sequential bibliography records, 294 unique Markdown
external URLs, 295 raw URI-like targets, 0 H1 issues, 0 unmatched fences,
0 missing local links, 0 article contract issues, `git diff --check`, and
`hugo --minify` with EN 26/RU 25/UK 25 pages and 64 static files per language.
Pre-existing `warning-goldmark-raw-html` on `/` remains; repeated observations
after supervisor turnover are still needed to validate the diagnostic construct.

Comparative-state-formation checkpoint: opened managed leaf `#241` under the
comparative-state-formation sub-epic. Added sources `#267–#272` on ancient
accounting and writing, non-written bookkeeping, fiscal legibility and direct
rule in colonial Mexico, divergent European fiscal responses to war, the
separation of fiscal capacity from executive constraints, and the political
settlement problem in early Pakistan. Added `StateFormationCase` and a five-part
test — information carrier, resource mobilization, intermediary, representation,
and persistence — to the state-formation article, historical regional dossier,
main governance map, evidence matrix and publication audit. The package now
explicitly separates `origin`, `function_switch` and `consolidation` and records
which sources support each phase rather than treating late bureaucracy as proof
of state origin.

Validation passed: 42 Markdown files, 20 article drafts, 272 sequential
bibliography records, 300 unique Markdown external URLs, 301 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: more matched cases are
needed before treating the new `StateFormationCase` as a validated measurement
instrument.

Matched-case coding checkpoint: opened managed leaf `#243` under the
state-formation-case-coding sub-epic and created
`research/state-formation-case-coding.md`. The protocol defines
`StateFormationCase`, separates `origin`, `function_switch` and `consolidation`,
uses `D/I/U/N` evidence statuses, and codes six rows covering Mesopotamia, Qin,
the Inca heartland, colonial Mexico, early modern Europe and early Pakistan.
It also records that legitimacy, representation, succession, exit and everyday
peripheral outcomes are weaker fields than task, resource and information
carrier. Navigation and open-question links were updated.

Validation passed: 43 Markdown files, 20 article drafts, 272 sequential
bibliography records, 300 unique Markdown external URLs, 301 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. The coding template is reproducible,
but its measurement validity still needs matched historical data.

Succession-and-persistence checkpoint: continued managed leaf `#245` under the
comparative-state-formation sub-epic. Added sources `#273–#276` on succession
shocks and civil-war risk in 28 European states over 800 years, succession
trade-offs across more than 300 Eurasian dynasties, long-run path dependence from
medieval heir availability, and bargaining windows in 293 European cities.
Added `SuccessionPersistenceCase` material to the state-formation article,
historical case passports, leadership article, evidence matrix, and matched-case
coding protocol; synchronized publication audit and open questions. The new
material separates succession rule, uncertainty, elite bargaining, bureaucratic
memory, capacity after change, representation, and voice after change. It records
that stable transition is not the same as legitimacy, inclusive representation or
good governance, and that one source remains a working paper.

Validation passed: 43 Markdown files, 20 article drafts, 276 sequential
bibliography records, 304 unique Markdown external URLs, 305 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: matched, time-ordered
cases are needed before treating `SuccessionPersistenceCase` as a validated
cross-domain measurement instrument.

State-capacity-and-everyday-outcomes checkpoint: opened managed leaf `#247`
under the new `state-capacity-and-everyday-outcomes` sub-epic. Added sources
`#277–#282` on county-level capacity and public goods in China, decentralized
service continuity in 83 Russian districts, extraction-linked infrastructure
under direct and indirect rule in India, local bureaucrat knowledge and
motivation in Nepal, expenditure and municipal budgets in Portugal, and the
long-run local-cooperation and openness trade-off in Vietnam. Added
`EverydayOutcomeCase` to the governance map, state-formation article, historical
case dossier and matched-case coding protocol; added evidence rows `E76–E81`, a
P1 backlog item, publication-audit rows, health and open-question updates.
The practical distinction is now explicit: extraction capacity, service access,
service quality, distribution, coercive burden, voice/appeal and persistence are
separate variables.

Validation passed: 43 Markdown files, 20 article drafts, 282 sequential
bibliography records, 310 unique Markdown external URLs, 311 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: service-level,
subnational and matched historical data are needed before treating
`EverydayOutcomeCase` as a validated cross-domain measurement instrument.

Closed-group-and-new-religious-movement checkpoint: continued managed leaf `#249`
under the closed-groups-and-new-religious-movements sub-epic. Added sources
`#283–#288` on commune survival, charismatic leader dominance and branch
structures, exit and identity transition among former Jehovah’s Witnesses,
qualitative post-exit experience in Finnish high-cost communities, social-media
support after disaffiliation, and multi-dimensional problems after leaving two
groups. Added `NRMExitCase` to the culture dossier and NRM article; it separates
durability, discipline, leader centralization, resource and relationship
dependence, exit mode, actual loss, external support and identity recovery. Added
evidence rows `E82–E87`, a P1 growth point, publication-audit rows, health and
open-question updates. The practical distinction is now explicit: group survival,
discipline and safe exit are separate outcomes; strict practice alone does not
prove abuse, and post-exit associations do not establish causal effects.

Validation passed: 43 Markdown files, 20 article drafts, 288 sequential
bibliography records, 316 unique Markdown external URLs, 317 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: matched, time-ordered
NRM cases with independent post-exit outcomes are needed before treating
`NRMExitCase` as a validated cross-domain measurement instrument.

Cross-domain-governance checkpoint: continued managed leaf `#252` under the new
cross-domain-governance-comparison sub-epic. Added sources `#293–#295` on four
governance structures in ten international advocacy NGOs, a matched comparison
of 153 majority-SOE and 153 private listed firms from 40 countries, and movement
access to corporate decision makers with co-optation risk. Added `CrossDomainCase`
to the comparative atlas and main governance map; it separates domain, public
task, information carrier, resource dependence, intermediary, authority,
implementation, voice, appeal, exit, persistence, capture and scope condition.
Added evidence rows `E92–E94`, updated publication audit, health and open
questions. The practical distinction is now explicit: a common mechanism can
transfer while its domain-specific scope condition changes who controls resources,
who translates signals and who can replace or appeal the intermediary.

Validation passed: 43 Markdown files, 20 article drafts, 295 sequential
bibliography records, 323 unique Markdown external URLs, 324 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: matched, time-ordered
cross-domain cases are needed before treating `CrossDomainCase` as a validated
transfer instrument.

Matched-case follow-up checkpoint: continued managed leaf `#250` under the
closed-groups-and-new-religious-movements sub-epic. Added sources `#289–#292` on
heterogeneous well-being profiles among 622 former members of fundamentalist
Christian communities, possible outside or family support in interviews with 31
former members, comparative disengagement across three NRMs and methodological
cautions on retrospective exit accounts, and different support pathways for
young people raised in closed groups. Extended `NRMExitCase` with age at entry,
external ties and available support; added evidence rows `E88–E91`, updated the
NRM article and culture dossier, publication audit and health. The practical
negative test is now explicit: long survival, disciplined membership and safe
exit are separate outcomes, while support is not equivalent to pressure.

Validation passed: 43 Markdown files, 20 article drafts, 292 sequential
bibliography records, 320 unique Markdown external URLs, 321 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: matched, time-ordered
NRM cases with independent post-exit outcomes are needed before treating
`NRMExitCase` as a validated cross-domain measurement instrument.

Mass-governance-capacity checkpoint: continued managed leaf `#254` under the
mass-governance-capacity-and-control sub-epic. Added sources `#296–#299` on state
capacity, digital surveillance and repression/public-goods trade-offs,
informational autocracy, and territorial variation in post-conflict control,
coercion and co-optation. Added `MassControlCase` to the main map and mass-
information article; it separates public task, service access and distribution,
information quality, intermediary, co-optation, repression, participation, voice,
appeal and exit. Added evidence rows `E95–E98`, publication-audit rows, health and
open-question updates. The practical distinction is now explicit: capacity and
compliance are not the same outcome, and apparent stability can be produced by
services, consent, dependence, information control or fear.

Validation passed: 43 Markdown files, 20 article drafts, 299 sequential
bibliography records, 327 unique Markdown external URLs, 328 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: matched, time-ordered
mass-governance cases are needed before treating `MassControlCase` as a
validated cross-domain measurement instrument.

Mass-participation-accountability checkpoint: continued managed leaf `#256`
under sub-epic `#255`. Added sources `#300–#307` on democratic rule choice,
visible elected leadership, post-conflict mobilization, informal accountability,
electoral and voice accountability, democratic farmer groups, long-term
community-driven development, and the organic/induced participation distinction.
Added `ParticipationCooperationCase` to the main map and the mass-action article;
it separates rule origin, leader selection, participant scope, information
visibility, voice, accountability, sanctions, mobilization, elite capture,
cooperation, service outcome, persistence and exit. Added evidence rows `E99–E106`,
publication-audit rows, health and open-question updates. The practical distinction
is now explicit: attendance or democratic form is not proof of influence, and
participation can improve a local task without producing durable governance.

Validation passed: 43 Markdown files, 20 article drafts, 307 sequential
bibliography records, 335 unique Markdown external URLs, 336 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: matched, time-ordered
participation cases are needed before treating `ParticipationCooperationCase` as
a validated cross-domain measurement instrument.

Legitimacy-compliance checkpoint: continued managed leaf `#258` under sub-epic
`#257`. Added sources `#308–#314` on fair enforcement, authority trust, sanction
severity, procedural justice, organizational justice, taxation legitimacy and
noisy enforcement. Added `LegitimacyComplianceCase` to the main map and the
power/authority article; it separates authority source, process fairness,
consistency, accuracy, voice, sanction severity, trust, intrinsic and extrinsic
compliance, correction, appeal and exit. Added evidence rows `E107–E113`,
publication-audit rows, health and open-question updates. The practical distinction
is now explicit: perceived legitimacy can support cooperation, but compliance
alone does not prove justice, accuracy or safe governance.

Validation passed: 43 Markdown files, 20 article drafts, 314 sequential
bibliography records, 342 unique Markdown external URLs, 343 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: matched, time-ordered
legitimacy cases are needed before treating `LegitimacyComplianceCase` as a
validated cross-domain measurement instrument.

Metric-governance checkpoint: continued managed leaf `#260` under sub-epic
`#259`. Added sources `#315–#320` on performance-management dysfunctions,
multitask incentives, hospital targets, team incentives and the politics of
measurement. Added `MetricGovernanceCase` to management dossier and main map;
it separates true goal, proxy, observability, task bundle, incentive, target
tightness, worker voice, gaming, information quality, quality, equity, safety,
learning, correction, exit and persistence. Added evidence rows `E114–E119`,
publication-audit rows, health and open-question updates. The practical
distinction is now explicit: a KPI is a control signal, not the goal; useful
target effects and null/distortion effects depend on task alignment, context,
horizon and correction.

Validation passed: 43 Markdown files, 20 article drafts, 320 sequential
bibliography records, 348 unique Markdown external URLs, 349 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: matched, time-ordered
metric-governance cases are needed before treating `MetricGovernanceCase` as a
validated cross-domain measurement instrument.

Institutional-learning checkpoint: continued managed leaf `#262` under
sub-epic `#261`. Added sources `#321–#335` on policy feedback, formal and
empirical policy experimentation, adaptive governance, organizational routines
and memory, knowledge transfer, intentional forgetting, succession and
routinization of charismatic authority. Added `InstitutionalLearningCase` to
the main map, management dossier and comparative case article; it separates
signal independence, trial, comparator, decision gate, discretion, memory,
transfer, leader turnover, voice, appeal, rollback, forgetting, capture,
short/long outcomes and persistence. Added evidence rows `E120–E132`,
publication-audit rows, health and open-question updates. The practical
distinction is now explicit: feedback changes the political field, learning
changes beliefs about causality, and adaptation stabilizes a new practice; a
pilot, archive or leader succession alone does not prove learning.

Validation passed: 43 Markdown files, 20 article drafts, 335 sequential
bibliography records, 363 unique Markdown external URLs, 364 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: matched, time-ordered
institutional-learning cases with independent outcomes, ordinary-regime
follow-up and observed rollback are needed before treating
`InstitutionalLearningCase` as a validated cross-domain measurement instrument.

Matched-longitudinal checkpoint: continued managed leaf `#264` under sub-epic
`#263`. Added sources `#336–#345` on randomized state-capacity rollout,
nine-year management follow-up, seven centuries of commons rules, historical
ABM validation, five decades of Nepal community forestry, state-reinforced
irrigation, enforcement backfire, drought coordination and health-shock forest
governance. Added `MatchedInstitutionalTrajectory` to the main map, management
dossier and comparative case article; it separates baseline, comparator,
intervention, implementation lag, signal quality, voice, appeal, exit, leader
turnover, process/quality/distribution/resource outcomes, persistence, rollback
and alternative explanations. Added evidence rows `E133–E142`,
publication-audit rows, health and open-question updates. The practical
distinction is now explicit: durable effect, partial decay, rule adaptation,
resource collapse and historical pattern reproduction are different outcomes.

Validation passed: 43 Markdown files, 20 article drafts, 345 sequential
bibliography records, 373 unique Markdown external URLs, 374 raw URI-like
targets, 0 H1 issues, 0 unmatched fences, 0 missing local links, 0 article
contract issues, `git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25
pages and 64 static files per language. Pre-existing
`warning-goldmark-raw-html` on `/` remains. Remaining: a smaller set of
genuinely comparable trajectories with consistent coding, independent data and
observed leader or rule turnover is needed before pooled causal claims.

Institutional-decay checkpoint: continued managed leaf #266 under sub-epic
`#265`. Added sources #346–#356 on corruption audits, elite capture, delegated
public services, organizational decline, retrenchment, commons degradation,
civil-service reform and reform reversal. Added `InstitutionalDecayCase` to the
main map, management dossier and comparative case article; it separates decay,
capture, independent task failure and recovery with baseline, stressor, information
loss, accountability, leadership change, distribution/resource outcomes, voice,
appeal, exit, rollback and persistence. Added evidence rows E143–E154 and updated
publication audit, health, open questions and source map.

Validation passed: health; 43 Markdown files; 20 article drafts; bibliography
1–356; evidence E143–E154; no H1, fence or local-link issues; `git diff --check`;
and `hugo --minify` with EN 26/RU 25/UK 25 pages and 64 static files per language.
Pre-existing `warning-goldmark-raw-html` remains.

Matched-decay checkpoint: managed leaf #268 under sub-epic #267 added
`DecayTrajectory` and a three-domain audit for randomized state-capacity rollout,
nine-year management follow-up and seven-century commons records. The audit keeps
process, function and distribution separate and records missing comparator,
long-run, turnover, voice, exit and rollback fields instead of pooling unlike
designs. Added evidence row E155 and updated open questions.

Safe-reform checkpoint: managed leaf #270 under sub-epic #269 added sources
#357–#359 on voice/silence, policy repeal lifecycle and limits to policy reversal.
Added `SafeReformCase` to the field guide and voice/appeal/exit article, with
protected signal, independent review, stop authority, correction, compensation,
exit, rollback, postmortem and persistence. Robodebt remains the documented
negative case for ignored adverse review and weak correction. Added evidence rows
E156–E158 and updated publication audit, health, open questions and source map.

Research-checkpoint log synchronization: managed leaf #272 under sub-epic #271
records actual outcomes, paths, validations and remaining limits for #266, #268
and #270. The main unresolved gap is live, comparable field evidence showing that
safe reform lowers correction latency without increasing veto overload or exit cost.

Longitudinal-NRM checkpoint: managed leaf #274 under sub-epic #273 added sources
#360–#363 on a three-year German NRM panel, post-founder succession in Shambhala,
adaptation of American spiritual communities, and four prospective US cohorts of
religious disengagement. Added `NRMExitTrajectory` to the culture dossier, NRM
article and main governance map; it orders pre-entry state, entry age, commitment,
dependence, leader change or conflict, exit, losses, external support, identity,
well-being and group outcome. Added evidence rows `E159–E163`, updated publication
audit, health, open questions and the P1 backlog. The direct NRM evidence is useful
but small (`N=71`, 9 leavers); succession cases measure organizations rather than
individual recovery, and the general-religion cohort is only a bounded comparator.

Validation passed: Kaneo health, 43 Markdown files, 20 article drafts, 363
sequential bibliography records, 163 evidence rows, 702 fence markers with no
unmatched fences, 393 unique Markdown URLs, 0 H1 issues, 0 missing local links,
`git diff --check`, and `hugo --minify` with EN 26/RU 25/UK 25 pages and 64 static
files per language. Pre-existing `warning-goldmark-raw-html` on `/` remains.
