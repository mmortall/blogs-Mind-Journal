# Agents map

> source: `CLAUDE.md`, `AGENTS.md`, `docs/llm-wiki.md`, `llm-wiki/index.md`

- `CLAUDE.md` is canonical repository memory and points to this wiki.
- `AGENTS.md` is a short compatibility pointer; it contains no second copy of
  project knowledge.
- `llm-wiki/index.md` is the central-routing entrypoint required by Cerebro.
- `.ai/knowledge.yaml` is the repository identity and ownership card.
- `.ai/knowledge.ps1` is the read-only router wrapper.
- No nested memory files were found outside these root files.

Kaneo-specific binding files are intentionally absent until a real project is
provisioned and read back from Kaneo.
